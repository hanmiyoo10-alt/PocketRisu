# Forward review — 2026-08-28 05:48 KST

## Cursor movement

- `nevaeh5379/HaejeokRisuai:main`: `58f1b42353e58054122bdb6e6d4e2e3597bc34cb` → `25abe86f7853ec172b3e6ae51fc43be1f4099bbb`
- Compared range: 3 commits; cursor moved forward only.
- Other Active sources checked in this pass remained at their authoritative cursors: `rpaddict/RisuBard`, `kwaroran/Risuai`, `kwaroran/Risuai-Next`, `PocketRisu/PocketRisu:develop`, `InoriNatsume/RisuVault`, `TripleHwang/RisuVault`, `seto-sama/PocketRisu-Kei`, `Nagase-Kotono/PocketRisu-kotono`, `tegy1117/Kei-Risu`, `PocketRisu-Alter/PocketRisu-Alter`.

## New / reclassified idea

### IMPORT-ANDROID-FILE-SLICE-STREAM

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P0`
- Lifecycle: `READY_TO_PORT`
- Source evidence: `nevaeh5379/HaejeokRisuai` `860280bcdd7e08c49a00a7391076de89d3c68e0e`
- PocketRisu evidence: `hanmiyoo10-alt/PocketRisu:main` still used `File.stream()` directly in `src/ts/process/processzip.ts` `CharXImporter.#toStream()` at inspection time.
- Benefit: Android WebView/content-provider-backed CharX files can be read through bounded `File.slice().arrayBuffer()` chunks instead of depending on a broken/unavailable `File.stream()` implementation; bounded memory behavior is preserved.
- Conflict/risk: localized EOF/off-by-one behavior only; no storage format, system package, DB, plugin reload, notification, or service-manager change.
- Validation need: focused Vitest with a `File` whose `stream()` throws, plus `pnpm check`.
- Follow-up: implementation branch created; draft PR remains gated on executable validation.

### Autonomous progression

- Helper dossier: `products/pocketrisu-helper-mod/docs/features/import-export/android-file-slice-stream/DESIGN.md`
- Personal fork branch: `feat/android-safe-charx-file-stream`
- Implementation commit: `f69d4f695d6d88af069257cc6d68eaba608ea5e2`
- Test commit: `607d9cdf251babdf783be6e90b8a13fef36cf9a4`
- Branch diff vs `main`: only `src/ts/process/processzip.ts` and `src/ts/process/processzip.test.ts`, +56/-5.
- Runner verification failure: attempted clean clone/test setup but the automation runner could not resolve `github.com`. This is an execution-environment/network failure, not a code/CI failure. GitHub combined status had no checks at inspection time.
- Draft PR: NOT OPENED because focused Vitest and `pnpm check` have not yet executed successfully.

## Evidence merged into existing ideas

### Backup restore format/admission and bounded restore

`8ebb91cfe2bddeeb5fe92c4151c64b3537e49071` adds a shared backup-entry classifier, rejects invalid path segments, distinguishes supported entries from fork extensions, drains unsupported extensions without persisting them, and reports `ignoredEntries`. Merge this as evidence into the existing restore-format-admission / path-validation family rather than creating a duplicate. Existing classification remains high-risk design/investigation only because restore is destructive.

### Restore memory / bounded SQL replacement

`25abe86f7853ec172b3e6ae51fc43be1f4099bbb` reduces restore peak memory by releasing the encoded database buffer, using `subarray()` rather than full-input `slice()` copies, and streaming SQL replacement through root/entity/message batches with partial-hydration fail-closed checks. Merge into existing streaming/bounded restore and partial-hydration persistence evidence. Do not port automatically: persistent restore replacement remains a high-risk storage/recovery boundary.

## Guardrails

No forced DB flush was introduced; `flushServerDbKeepalive()` no-op remains untouched; targeted V3 reload, runit, and no-server-phone-notification rules are unaffected.

## Historical coverage

`HISTORICAL_BACKFILL_COMPLETE_THROUGH` unchanged. This pass processed forward traffic only and does not prove complete common historical coverage.
