# Forward review — 2026-08-28 01:40 KST

Authoritative starting cursors came from `notes/external-risu-dev-watch.md`. No cursor was moved backward.

## 1. `nevaeh5379/HaejeokRisuai`

Cursor: `1d39547e04f529deb7da2ca7d661d371a81cbd20` → `5babb10c9802e287235111f4aa2b75121cee35fd` (5 commits).

### Merged evidence — restore format admission / bounded restore materialization

Source evidence:

- `90af43ed77ac53b5dd7773205ed11beaf1d0e5b9` — chunked Tauri file import + transaction progress events.
- `36ad93e42e9a7eff3e8391993844dc88e74664ce` — distinguish parser-consumed logical bytes from buffered-stream prefetch when validating restore size.
- `fb6e868208d140611ec514453710a444750e9b85` — pure-Java framed backup container codec with streaming and entry-name/size validation.
- `e9c37503a9b694ca9a06b57f8dc6e8e2e3762fd4` — native SQLite streaming restore plus dedicated 32 MiB low-memory CI tests.
- `5babb10c9802e287235111f4aa2b75121cee35fd` — more granular progress callbacks without changing restore ownership.

Classification (existing idea, evidence merged):

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `HIGH`
- Size: `M`
- Evidence: `MEDIUM`
- Risk: `HIGH`
- Dependencies: current PocketRisu restore-format/native-bridge/staging/transaction ownership audit and direct reproduction
- Priority: `P2`
- Lifecycle status: `DESIGN_NEEDED`
- Benefit: fail-closed format admission, lower peak restore memory, stronger structural validation, testable low-memory behavior.
- Conflict/risk: restore is destructive; wrong classifier/byte accounting/transaction decomposition can reject valid backups or publish partial state.
- Validation need: current accepted-format matrix, parser-consumed-vs-prefetch tests, truncation/impossible-size/path tests, atomic rollback, explicit constrained-memory CI target.
- Follow-up: helper dossier improved at `products/pocketrisu-helper-mod/docs/features/backup/restore-format-admission/DESIGN.md`; no implementation branch/PR because Risk is HIGH/destructive restore gate applies.

Key invariant added: structural size validation must use bytes consumed by the format parser, not transport-buffer read-ahead. “Streaming” claims should be backed by an explicit constrained-memory test, not only code shape.

## 2. `TripleHwang/RisuVault`

Cursor: `9765fd28b079a1d52a8dee87410d57f6fc0b790d` → `5afa95a9379ef45ef8484617a5407726d14e5f2b` (6 commits).

### Merged evidence — continuous chat history / hydration contract

Source evidence:

- `08f84acc326d2288f2895f06e5aead17dc1ead1a` — terminal cursor client/server contract mismatch made the oldest page impossible to reach; tied positions also broke strict ordering assumptions. Real server/client contract tests were added.
- `a494f246e88b7617e43c58bd9bf047f3c101e6d7` — global DB revision was too broad a consistency guard; unrelated writes aborted unchanged-chat hydration. The fix compares chat-scoped shared state and degrades safely when the advisory counter drifts.

Classification (existing idea, evidence merged):

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `MEDIUM`
- Size: `M`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: PocketRisu current paging/hydration ownership and current terminal-cursor contract reproduction
- Priority: `P1`
- Lifecycle status: `DESIGN_NEEDED`
- Benefit: reach oldest history reliably, avoid infinite/refused terminal paging, prevent unrelated saves from blanking a chat during hydration.
- Conflict/risk: wrong cursor/tie semantics can duplicate, skip, reorder, or pin messages; too-weak consistency guard can accept a truly stale target chat.
- Validation need: real server-produced fixtures through the actual client validator, tied-position boundary tests, unrelated-write concurrency test, target-chat-change test, rapid switch/stale completion tests.
- Follow-up: helper dossier strengthened at `products/pocketrisu-helper-mod/docs/features/chat/continuous-history-loading/DESIGN.md`; no implementation until PocketRisu reproduces the relevant boundary.

### New idea — structured regex/action flag editing

Source evidence: `3836b0c37b260dd573124ef85b00920d268f0921`.

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: PocketRisu current script flag representation/editor/runtime ownership audit
- Priority: `P1`
- Lifecycle status: `DESIGN_NEEDED`
- Benefit: prevents a one-letter regex-flag toggle from corrupting embedded action tags such as `<cbs>`; aligns runtime/editor normalization and surfaces broken regexes instead of silently disabling scripts.
- Conflict/risk: current PocketRisu may already have a structured representation; blindly normalizing legacy values could alter user scripts.
- Validation need: inspect current representation; regression tests for `<cbs>s`, `<move_top>m`, whitespace/fallback behavior, unknown tags, invalid regex diagnostics, byte-preserving healthy round trips.
- Follow-up: assistant-owned dossier created at `products/pocketrisu-helper-mod/docs/features/scripts/structured-regex-flags/DESIGN.md`. Promote only if current PocketRisu reproduces the mixed-string bug; no source branch/PR yet.

### Merged evidence — plugin update durability/transport safety

Source evidence: `6af5e68d765a7725c365e1fd0cd971264451f87d`.

This strengthens the existing plugin-update durability/update-fetch family rather than creating a duplicate: avoid unsafelisted `Cache-Control` request headers that induce CORS preflight failures; server-relayed self-declared update URLs must be HTTPS and reject private/loopback/link-local destinations; successful update must become durable before reload; verification must distinguish source URL movement from malicious identity change; failed installs must not leave optimistic cache state; failures should expose the failing stage.

Classification retained pending PocketRisu ownership audit:

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `S`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: current PocketRisu plugin updater/fetch relay/durable-save ownership audit
- Priority: `P0`
- Lifecycle status: `DESIGN_NEEDED`
- Benefit: update checks actually reach compatible hosts, SSRF surface stays bounded, installed plugin survives reload, failures become diagnosable.
- Conflict/risk: URL policy/redirect handling and plugin identity rules are security-sensitive; preserve targeted V3 reload and permission boundaries.
- Validation need: CORS/no-preflight tests, SSRF host-resolution/redirect tests, durable-save-before-reload test, moved-update-URL identity test, failed-update cache rollback.
- Follow-up: inspect existing `plugin-update-fetch` and `plugin-targeted-reload` dossiers before any implementation; no branch/PR in this run.

## Autonomous progression performed

- Improved helper design `backup/restore-format-admission` with parser-consumed byte accounting, explicit low-memory CI evidence, streaming/progress invariants, and PR decomposition.
- Improved helper design `chat/continuous-history-loading` with terminal cursor parity, tied-position rules, chat-scoped hydration consistency, and real server/client contract testing.
- Created helper design `scripts/structured-regex-flags` for the new low-risk candidate.
- No source implementation branch or personal draft PR was opened: restore is HIGH-risk/destructive; chat paging still has unresolved PocketRisu ownership dependency; regex candidate still needs current-code reproduction; plugin update touches security-sensitive URL/identity boundaries.

## Historical/discovery marker

Forward traffic was meaningful, so no historical coverage marker was advanced. `HISTORICAL_BACKFILL_COMPLETE_THROUGH` remains unchanged.
