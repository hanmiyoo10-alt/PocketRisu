# Forward review — rpaddict/RisuBard v0.9.14

Reviewed source: `rpaddict/RisuBard:main`

Previous authoritative cursor: `5340a96440689893bedf6ad6de5a1d30b7c231f0`

Reviewed through: `e47bc14090618450b271eaac2a1c1891757ac535`

## Forward range

The source advanced by one commit. `e47bc14090618450b271eaac2a1c1891757ac535` is a release commit with substantive code/docs changes, not only a version bump.

## Meaningful transferable evidence

### INTERCHANGE-SCHEMA-ROUNDTRIP-COMPAT — evidence merge

RisuBard v0.9.14 preserves Grimoire state in the app-owned `data.extensions.risubard.bardLore` namespace for PNG/JSON/CHARX export while keeping standard `character_book` unchanged. Its release contract explicitly treats the private extension as optional: another app may ignore the namespace, and malformed Grimoire extension data must not prevent the standard lorebook import path.

This is the same underlying portability idea already tracked by `INTERCHANGE-SCHEMA-ROUNDTRIP-COMPAT`, not a new backlog item. Merge the source evidence rather than creating a duplicate.

Current official PocketRisu `278251f85a19bfdfd4cf3faae780e62682878f9e` already demonstrates part of the invariant: `createBaseV2()` owns `data.extensions.risuai`, preserves unrelated extension namespaces, and keeps standard `character_book` separate. No current PocketRisu regression was reproduced in this run, so the lifecycle remains `DESIGN_NEEDED`; there is no safe implementation candidate yet.

Classification remains:

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `S`
- Evidence: `MEDIUM`
- Risk: `LOW`
- Dependencies: current PocketRisu schema inventory plus a reproduced round-trip loss or malformed-private-extension failure before implementation
- Priority: `P1`
- lifecycle status: `DESIGN_NEEDED`
- source evidence: `seto-sama/PocketRisu-Kei@294c6e4828861b3590bbd6b40d26a259af12ed97`; `rpaddict/RisuBard@e47bc14090618450b271eaac2a1c1891757ac535`; official PocketRisu comparison at `278251f85a19bfdfd4cf3faae780e62682878f9e`
- benefit: preserve app-specific portable state without breaking interoperable Character Card/lorebook semantics or silently dropping variant-owned data
- conflict/risk: private extension data must not become authoritative for standard import; malformed optional data must be failure-contained; unknown namespaces must not be guessed into PocketRisu ownership
- validation need: field inventory, round-trip fixtures, unknown-extension preservation fixture, malformed-PocketRisu-extension + valid-standard-card fixture, source-object immutability
- follow-up: keep assistant-owned design dossier current; move to `READY_TO_PORT` only after a concrete PocketRisu loss/failure is reproduced and the isolated fix/rollback are explicit

## Other release changes

The release also adds chat-local toggle preset baselines/reset UI, lorebook folder display ordering, Grimoire labels/help, and BardWiki first-message analysis. In this bounded pass these were either product-specific or overlapped existing state-scope/bounded-context ideas strongly enough that they do not justify separate PocketRisu backlog entries yet.

## Progression performed

- reviewed only the commit newer than the authoritative RisuBard cursor;
- deduplicated the portability evidence into the existing interchange-schema idea;
- improved the helper-repo design dossier with namespaced-extension and malformed-optional-extension invariants;
- no production code branch/test/personal PR was started because the item remains `DESIGN_NEEDED` with a reproduction dependency;
- advance only the RisuBard forward cursor to the reviewed HEAD.

`HISTORICAL_BACKFILL_COMPLETE_THROUGH` is unchanged; this was a forward review and does not prove complete historical coverage.
