# Forward review — RisuBard c1259e98 → f3d9bd4c

Reviewed source: `rpaddict/RisuBard:main`

- Previous authoritative cursor: `c1259e98bbb6b27697e2e891a96b5cb230c5bff8`
- Reviewed HEAD: `f3d9bd4c2d532e55f77ed7c87c83cee733dae807`
- Range shape: one direct release commit (`f3d9bd4c`, `release: prepare 0.9.11`) containing a large bundled change set.

## Meaningful merged evidence

### Active-chat paging: keep the character first message available as a bounded context anchor

Do not create a new paging feature. Merge this evidence into the existing active-chat paging / bounded continuous history-loading idea.

Source evidence in `f3d9bd4c2d532e55f77ed7c87c83cee733dae807`:

- `DefaultChatScreen.svelte` renders the first/greeting message on page 0 as before.
- On later pages it keeps the first message available behind an explicit expand/collapse control rather than permanently rendering it.
- Later pages default to collapsed (`firstMessageCollapsed = true`).
- The collapsed state is reset when the pagination identity changes, preventing a prior chat/page session from leaking UI state into a new pagination identity.
- Tests assert the first-message anchor is present, later pages default collapsed, the toggle is placed after message content, and `aria-expanded` tracks the state.

Why it matters for PocketRisu:

When active-chat paging/windowing eventually lands, removing the greeting/first-message context entirely from later pages makes character context harder to inspect, while rendering it unconditionally on every page increases DOM/render cost. The RisuBard implementation demonstrates a middle path: preserve context accessibility without defeating the bounded-window goal.

This is evidence for a UX/ownership invariant, not authority to copy RisuBard's pagination architecture.

## Classification (merged into existing idea)

- Feature-ID / idea boundary: `ACTIVE-CHAT-PAGING-FIRST-MESSAGE-CONTEXT-ANCHOR` (evidence merged into active-chat paging / `continuous-history-loading`; do not maintain as an independent competing paging feature)
- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `MEDIUM`
- Size: `M`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: PocketRisu current chat-window/storage ownership audit; actual bounded paging/window architecture; direct reproduction/measurement before implementation
- Priority: `P1`
- Lifecycle: `DESIGN_NEEDED`
- Source evidence: `rpaddict/RisuBard@f3d9bd4c2d532e55f77ed7c87c83cee733dae807` (`DefaultChatScreen.svelte`, `ChatPaginationConnections.test.ts`)
- Benefit: retain first-message/greeting context across later history pages without permanently adding it to every rendered page
- Conflict / risk: unconditional duplicate rendering can undermine bounded DOM goals; stale collapse state can leak across chat/page identity changes; anchor must not become a second canonical message instance for edit/search/plugin semantics
- Validation need: page 0 first message remains visible; later pages are collapsed by default; explicit expansion is accessible; pagination/chat identity changes reset transient collapse state; expand/collapse does not alter message positions/IDs or counted page membership; bounded render count remains intact
- Follow-up: incorporate this invariant into the existing continuous-history-loading design; keep implementation blocked until the underlying PocketRisu paging/window owner is located and validated

## Other reviewed release content

The release also contains RisuBard-specific Arca log workflow and BardWiki changes plus responsive min-width polish. They were reviewed as source-specific or lower-priority evidence and were not promoted to separate durable PocketRisu ideas in this bounded pass.

## Cursor decision

`f3d9bd4c2d532e55f77ed7c87c83cee733dae807` is a direct descendant of the previous RisuBard cursor and this release commit was reviewed. Advance only the RisuBard forward cursor to this HEAD. Historical markers are unchanged.
