# Forward review — 2026-08-28 07:35 KST

## Cursor movement

- `nevaeh5379/HaejeokRisuai`: `25abe86f7853ec172b3e6ae51fc43be1f4099bbb` → `1c50be425da01314f56068f57bcacd0254c76f99`
- All other Active sources checked in this run matched their durable `Last reviewed HEAD` and were not moved.

Reviewed Haejeok commits newer than the prior cursor:

- `c71c64fd7f4c6c41cfcf10b72ea09ce23416dd25` — Android SQLite restore progress/error handling
- `47daa0c931db96f1301a4852a130b347b4818617` — replace-entities SQL tail-delete suppression
- `7a7271b3213fd51ba23496716f9b9b98df8bbb12` — replace-entities metadata cleanup suppression
- `7aea7329c2ce4323c9a3bf4b72cf5adc88cc03a1` — throttled thumbnail cache pruning/touch I/O
- `51513f9edef1a74fe0b718943da68fcdc8a149e2` — immutable local asset serving to WebView
- `91dc8cb0aafce9b2083a9cc0d9ece115cc69669b` — bounded byte-budget native thumbnail cache + batch preparation
- `1c50be425da01314f56068f57bcacd0254c76f99` — Android custom-extension file-picker compatibility

---

## New / reclassified idea

### Android custom-extension file-picker compatibility

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P0`
- Lifecycle status: `READY_TO_PORT`
- Source evidence: `nevaeh5379/HaejeokRisuai@1c50be425da01314f56068f57bcacd0254c76f99`
- PocketRisu evidence: personal fork `main@39b3df07ae1189a4237407ae6b7ee9067c9c39e5`, `src/ts/util.ts`
- Benefit: Android content/file pickers can hide app-owned formats such as `.charx`/`.risum` when unknown extensions are placed in the HTML `accept` filter. Omitting the restrictive hint only for affected Android custom-extension flows makes those files selectable while retaining PocketRisu's existing post-selection extension validation.
- Conflict/risk: chooser may display more files on affected Android flows, but existing filename-extension validation remains authoritative; no content parser/storage behavior changes.
- Validation need: focused Android/non-Android chooser-helper tests, wrong-extension rejection regression, project check/typecheck. No draft PR should be opened until executable validation succeeds.
- Follow-up: Feature dossier created at `hanmiyoo10-alt/-/products/pocketrisu-helper-mod/docs/features/import-export/android-custom-extension-picker/DESIGN.md`; isolated branch boundary `feat/android-custom-extension-picker` created from `main@39b3df07...`. Source mutation was intentionally withheld because the execution environment could not resolve `github.com` for a clean checkout/test run. This is a validation-environment blocker, not a code/CI failure.

### Why Evidence is HIGH

The external commit is a direct bug fix with a clear causal explanation, and the personal fork was inspected directly: `selectFileByDom()` still writes every requested extension into `fileInput.accept` and then independently filters selected filenames by extension. The same ownership boundary therefore exists in PocketRisu.

---

## Evidence merged into existing ideas

### Bounded native thumbnail cache

Merged, no duplicate idea created:

- `91dc8cb0aafce9b2083a9cc0d9ece115cc69669b`: batch thumbnail preparation, de-duplication, dedicated thumbnail URL, byte-budget cache (128 MiB limit / 96 MiB target).
- `7aea7329c2ce4323c9a3bf4b72cf5adc88cc03a1`: throttle pruning and access-time touches to avoid filesystem I/O amplification while scrolling.
- `51513f9edef1a74fe0b718943da68fcdc8a149e2`: local immutable asset serving reinforces avoiding unnecessary JS/Base64 materialization.

Existing classification remains `NO_SYSTEM_UPDATE / HIGH / MEDIUM / M / Evidence MEDIUM / Risk MEDIUM / P1 / DESIGN_NEEDED` because PocketRisu-specific physical memory/decode benefit is still unmeasured.

Helper dossier strengthened at `products/pocketrisu-helper-mod/docs/features/mobile/native-thumbnail-cache/DESIGN.md`.

### Restore / persistence evidence

`c71c64fd...`, `47daa0c...`, and `7a7271b...` were treated as evidence for existing bounded restore / partial-hydration persistence / SQL replacement correctness families. No duplicate idea was created. Restore remains high-risk and design/investigation-only under the automation gate.

---

## Autonomous progression performed

1. Read canonical registry, classification policy, and idea ledger before discovery.
2. Checked all Active source HEADs; only Haejeok advanced.
3. Compared exactly the seven commits newer than the Haejeok cursor.
4. Inspected current personal-fork `src/ts/util.ts` and confirmed the same chooser ownership boundary.
5. Created helper dossier `ANDROID-CUSTOM-EXTENSION-PICKER` with `READY_TO_PORT` acceptance/rollback.
6. Created fresh personal-fork branch `feat/android-custom-extension-picker` from `main@39b3df07...`.
7. Attempted clean checkout for modify/verify; environment failed at DNS resolution for `github.com`. No source mutation was made and no draft PR was opened.
8. Strengthened the existing native-thumbnail-cache dossier with new byte-budget and maintenance-throttling evidence.

## Guardrail check

No DB lifecycle flush, `flushServerDbKeepalive()`, save/integrity optimization, V3 reload, runit/PM2, server-phone notification, storage migration, destructive restore, or credential/session boundary was modified.

## Historical marker

`HISTORICAL_BACKFILL_COMPLETE_THROUGH` unchanged. This was a forward-traffic run and provides no new proof of complete cross-source historical coverage.
