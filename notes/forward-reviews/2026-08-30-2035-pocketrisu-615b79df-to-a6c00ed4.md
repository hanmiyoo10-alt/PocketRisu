# Forward review — PocketRisu 615b79df → a6c00ed4

- Reviewed: 2026-08-30 20:35 KST
- Source: `PocketRisu/PocketRisu:develop`
- Previous cursor: `615b79df3375bf9db2924a8003f61a747721c725`
- Reviewed HEAD: `a6c00ed44aaf639b53f2195e58e9f670b24f079a`
- Range: 5 commits ahead; merge-only `d90d26094682ce0f85220348c9e711e6e5704a54` carries no separate idea.

## Meaningful ideas

### `PLUGIN-LAZY-ASSET-COMPAT-SNAPSHOT` — `4a70de8d3c0853704dab22dedd63f19a600c42e0`

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `MEDIUM`
- Size: `S`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: PocketRisu lazy asset-manifest architecture, bounded full-manifest cache, and v3 character API compatibility contract; personal fork `develop@e57c0435` predates this architecture slice.
- Priority: `P1`
- Lifecycle status: `HOLD`
- Source evidence: official PocketRisu issue/fix #80 at `4a70de8d`; plugin character reads hydrate one detached character snapshot to the legacy `additionalAssets` shape while DBState stays lazy; unchanged writes restore the descriptor and changed asset lists stay inline for normal recanonicalization.
- Benefit: storage laziness does not silently break existing v3 plugins or turn plugin field-only edits into asset-manifest rewrites.
- Conflict/risk: crossing lazy-storage/plugin compatibility boundaries can alias cache state, duplicate large arrays, or accidentally persist hydrated compatibility data.
- Validation need: read compatibility, detached-copy/non-aliasing, load-failure degradation, unchanged-list descriptor restoration, changed-list inline behavior, and preservation of targeted V3 reload.
- Follow-up: keep as a required compatibility invariant for any future lazy asset-manifest sync/port; do not cherry-pick before its storage dependencies exist.

### `CBS-SCRIPT-INTRODUCED-ASSET-HYDRATION` — `54813802f7b33ac437d097d3538b96087014fb82`

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `MEDIUM`
- Size: `S`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: lazy asset-manifest cache plus current CBS/script pipeline; personal fork does not yet have the reviewed owner set.
- Priority: `P2`
- Lifecycle status: `HOLD`
- Source evidence: synchronous CBS asset-list tokens can be introduced by edit/display scripts after the original message scan; the fix pre-scans message and script templates, mirrors token normalization, and hydrates only missing content-addressed manifests.
- Benefit: script-generated `assetlist`/display/module-list tokens expand reliably without globally hydrating all assets.
- Conflict/risk: an incomplete detector causes silent unexpanded tokens; an over-broad detector defeats bounded lazy loading.
- Validation need: accepted token spellings/separators, script-introduced tokens, cache-hit no-fetch behavior, missing-manifest failure degradation, and bounded hydration.
- Follow-up: revisit with the lazy-manifest architecture; merge with that design rather than creating a second hydration authority.

### `SNAPSHOT-REQUIRES-DURABLE-SOURCE` — `a838a95781993d342c96fa36fa3bb87bd2a035d5`

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: newer plugin-storage snapshot-map architecture; personal fork `develop@e57c0435` has an older backup path without the same map owner.
- Priority: `P1`
- Lifecycle status: `HOLD`
- Source evidence: a fresh install could attempt a backup before `database/database.bin` existed; blob copy skipped the missing source while plugin snapshot metadata was still written, creating an orphan map. The fix refuses snapshot materialization without the authoritative DB source and changes tests to assert one map row per listed snapshot rather than timing-sensitive exact counts.
- Benefit: prevents metadata-without-snapshot artifacts and makes tests assert the durable relationship instead of timestamp coincidence.
- Conflict/risk: porting only the guard into an older backup architecture would be dead/misleading code.
- Validation need: fresh-install first import, missing DB, normal snapshot, cooldown semantics, one map per listed snapshot, and GC behavior.
- Follow-up: preserve as an invariant if/when the plugin-storage snapshot-map architecture is synced; no isolated implementation now.

### `INLAY-DIMENSION-LOOKUP-REMOUNT-OWNERSHIP` — `a6c00ed44aaf639b53f2195e58e9f670b24f079a`

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `MEDIUM`
- Size: `S`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: current fast inlay cache/dimension lookup path introduced after personal fork base; matching DOM/cache ownership must exist first.
- Priority: `P2`
- Lifecycle status: `HOLD`
- Source evidence: a cache entry can be published before dimensions arrive; a remounted image may hit that cache and bypass the original resolve queue. The fix keeps one pending dimension lookup per inlay id, reconnects remounted images, deduplicates ids, and validates both user-writable marker and current cached `src` before applying dimensions.
- Benefit: avoids layout jumps for remounted cached inlays while retaining non-blocking image loading.
- Conflict/risk: stale async completions or trusting a user-writable marker alone could size the wrong element; unresolved lookups can retain DOM/cache state.
- Validation need: remount during pending lookup, src mismatch, disconnected element, hide-images transition, duplicate ids, legacy no-dimension assets, lookup failure, and cleanup after settle.
- Follow-up: treat as ownership guidance when the matching fast-inlay path reaches the personal fork; do not port the helper in isolation.

## Progression decision

All four ideas are source-proven, but the personal fork `develop@e57c0435018646800566f2158fd1a9fa12caa9e2` predates their required architecture slices. Priority does not override dependencies, so none qualifies for autonomous implementation in this run. No feature branch, production commit, test run, or personal PR was created.

## Cursor decision

The entire forward range from the previous cursor through `a6c00ed44aaf639b53f2195e58e9f670b24f079a` was reviewed. Advance only the official PocketRisu source cursor to that HEAD; do not change historical-backfill coverage markers.