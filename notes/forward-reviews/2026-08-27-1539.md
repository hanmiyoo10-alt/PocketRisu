# Forward review — 2026-08-27 15:39 KST

## Source

- Repository: `nevaeh5379/HaejeokRisuai`
- Previous authoritative cursor: `afcfae25fb4de5171a3847fa6f4f6e9d9dff9d13`
- Reviewed new HEAD: `30a927122c4306a356022ca76ad2053639c5bc1b`
- Meaningful commit: `30a927122c4306a356022ca76ad2053639c5bc1b`

## Deduplicated idea: explicit backup quiescence gate before snapshot/publish

This evidence is merged into the existing backup-integrity/pre-publish-verification family rather than creating an unrelated duplicate.

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `S`
- Evidence: `MEDIUM`
- Risk: `MEDIUM`
- Dependencies: audit current PocketRisu backup snapshot ownership and pending-write visibility; preserve existing save/integrity optimizations
- Priority: `P0`
- Lifecycle status: `DESIGN_NEEDED`
- Source evidence: `nevaeh5379/HaejeokRisuai` `30a927122c4306a356022ca76ad2053639c5bc1b`
- Benefit: an explicit user-requested backup can fail closed instead of snapshotting while known durable-store writes are still pending.
- Conflict/risk: the same source commit also adds `pagehide` flushing. That portion directly conflicts with the PocketRisu guardrail against reintroducing forced DB/store flushes on `visibilitychange` / `pagehide` and is **not** a port candidate. A badly designed quiescence gate can also deadlock, wait forever, duplicate expensive writes, or falsely block backups.
- Validation need: inject pending and failing writes around an explicit backup request; prove the backup waits/flushes only through the already-owned durable-store boundary, aborts on unresolved pending work, creates no second full payload copy, and does not add lifecycle-event flushing.
- Follow-up: improve `BACKUP-PRE-PUBLISH-INTEGRITY-VERIFIER` design so quiescence is a separate precondition before snapshot assembly; inspect current PocketRisu save/backup ownership before promotion. No source implementation until that dependency is resolved.

## Additional evidence retained, not promoted independently

The same commit also contains useful storage lessons: failed debounced root commits are re-queued with bounded exponential retry without overwriting a newer pending value; concurrent SQL commits are serialized/rebased; legacy migration source data is retired only after successful replacement; replace-all refuses partially hydrated character/chat state. These reinforce existing serialized-commit, migration-safety, and deferred-hydration invariants. They are evidence only because PocketRisu's current storage architecture must remain authoritative.

## Guardrail decision

- Explicit backup action: quiescence/pending-write check is worth design investigation.
- Explicit native app exit: separate ownership review required; not part of the first backup slice.
- `pagehide` / `visibilitychange`: **do not adopt forced flush behavior**.
- `flushServerDbKeepalive()`: remains no-op unless separately reviewed.

## Progression

- Durable forward review recorded here.
- Existing helper-repo backup verifier dossier is updated with the new evidence and a strict separation between explicit backup quiescence and lifecycle flushing.
- No implementation branch or PR: unresolved PocketRisu ownership dependency keeps lifecycle `DESIGN_NEEDED`.
- Forward cursor may advance to `30a927122c4306a356022ca76ad2053639c5bc1b`; no cursor moves backward.
- `HISTORICAL_BACKFILL_COMPLETE_THROUGH` unchanged.
