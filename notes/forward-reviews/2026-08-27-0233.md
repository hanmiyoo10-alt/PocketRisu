# Forward review — 2026-08-27 02:33 KST

## Source advance

- Source: `TripleHwang/RisuVault`
- Previous cursor: `acabebafd1d295bede3093e2cf28ebfaab0cfdc1`
- Reviewed HEAD: `e750a66f513bd4a4161df7833367058363d949ac`
- Range: 4 commits, including `975b257b35b40203c4c875fbd2563c55ddf1083d` and `7d23e41425555fab02d8ef7a5b06269520b6f9be`.

## Deduplicated idea evidence

### SQL migration verification should prove the committed revision without immediately rematerializing the entire database

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `HIGH`
- Size: `M`
- Evidence: `MEDIUM`
- Risk: `HIGH`
- Dependencies: a future PocketRisu relational/domain SQL migration; exact authoritative revision contract; migration backup/fallback contract
- Priority: `P2`
- Lifecycle: `HOLD`
- Source evidence: `TripleHwang/RisuVault` `975b257b35b40203c4c875fbd2563c55ddf1083d`
- Benefit: if PocketRisu later migrates a large legacy graph into relational SQL, verify success via a lightweight bootstrap/revision observation and retain the exact just-imported graph for the first runtime instead of downloading/reconstructing the whole database again.
- Conflict/risk: accepting an incompletely persisted migration would be destructive. The lightweight proof is valid only when the commit acknowledgement and observed revision are authoritative and the legacy source remains available for fallback.
- Validation need: injected mismatch/restart tests, backup-before-migrate proof, revision equality, failed verification falls back to legacy, and no full snapshot read on the successful Node path.
- Follow-up: preserve as evidence for the existing storage-architecture/migration design family; do not implement against current PocketRisu KV/chunk storage.

### Migration durability boundary: checkpoint only after successful large replacement, and preserve graceful close semantics

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `HIGH`
- Size: `S`
- Evidence: `MEDIUM`
- Risk: `HIGH`
- Dependencies: same future relational SQL migration boundary; shutdown ownership; measured WAL behavior
- Priority: `P2`
- Lifecycle: `HOLD`
- Source evidence: `TripleHwang/RisuVault` `7d23e41425555fab02d8ef7a5b06269520b6f9be`, merge `e750a66f513bd4a4161df7833367058363d949ac`
- Benefit: a large replace-all migration can be checkpointed after its transaction commits so restart durability does not depend on retaining a large WAL; graceful shutdown can checkpoint/close the relational store.
- Conflict/risk: this must **not** become a forced browser lifecycle DB flush. PocketRisu guardrails keep `flushServerDbKeepalive()` no-op and forbid visibilitychange/pagehide full flush. Current PocketRisu already exposes `checkpointWal()` in `server/node/db.cjs`; any future use must stay server-owned and serialized.
- Validation need: WAL/reopen test after successful replacement, failure-before-commit test, SIGTERM/SIGINT server shutdown test, and proof no browser lifecycle hook is added.
- Follow-up: merge as evidence into future SQL migration/storage dossier; no autonomous implementation now.

### Transaction-local prepared statement reuse during bulk SQL commit

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `MEDIUM`
- Risk: `LOW`
- Dependencies: relational bulk SQL commit path must exist and profiling must show prepare overhead
- Priority: `P1`
- Lifecycle: `HOLD`
- Source evidence: `TripleHwang/RisuVault` `975b257b35b40203c4c875fbd2563c55ddf1083d`
- Benefit: reuse identical SQL shapes inside one large migration transaction without letting prepared statements survive rollback/reopen boundaries.
- Conflict/risk: premature optimization in current PocketRisu, whose main persistence architecture is KV/chunk based rather than this relational commit path.
- Validation need: migration benchmark and assertion that the cache is scoped to one commit only.
- Follow-up: retain as a bounded optimization slice if a relational migration path is adopted and measured.

### Runtime-mode injection must cover direct `/index.html` navigation before static middleware

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `MEDIUM`
- Risk: `MEDIUM`
- Dependencies: reproduce whether PocketRisu installed WebView/shortcut/direct navigation can bypass its current server runtime injection
- Priority: `P0`
- Lifecycle: `DESIGN_NEEDED`
- Source evidence: `TripleHwang/RisuVault` `7d23e41425555fab02d8ef7a5b06269520b6f9be`, merge `e750a66f513bd4a4161df7833367058363d949ac`
- Benefit: prevents a direct `/index.html` navigation from selecting browser-side storage/runtime semantics when the same standalone server should own the session.
- Conflict/risk: route-order changes can alter static caching or startup semantics; PocketRisu may already have a different injection mechanism.
- Validation need: inspect current root/index serving, reproduce both `/` and `/index.html`, verify identical runtime flags and storage selection, and preserve asset caching/auth behavior.
- Follow-up: create helper-repo design dossier and keep `DESIGN_NEEDED` until PocketRisu-specific reproduction resolves the dependency.

## Guardrail check

No source code was changed. The migration/storage ideas remain investigation/design only because they are storage-architecture work and/or high risk. No forced DB flush, `flushServerDbKeepalive()` change, PM2, Android notification, device package/runtime migration, or destructive migration was introduced.

`HISTORICAL_BACKFILL_COMPLETE_THROUGH` is unchanged; this was forward review, not proof of complete historical coverage across all tracked sources.
