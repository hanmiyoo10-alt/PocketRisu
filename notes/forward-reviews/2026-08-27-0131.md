# Risu-family forward review — 2026-08-27 01:31 KST

This file is a durable handoff/addendum for `notes/external-risu-ideas.md`. It records this bounded forward pass without moving any source cursor backward.

## Meaningful changes

### 1. HaejeokRisuai — bounded native thumbnail path for Android character/media grids

- Source: `nevaeh5379/HaejeokRisuai:main`
- Key commit: `1f5bb1328c4486d968c3d7c9781f036486dd0b4c`
- Related commit: `aef7e7479a24dbc1f2adf95a40131de8dedd62ff`
- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `MEDIUM`
- Size: `M`
- Evidence: `MEDIUM`
- Risk: `MEDIUM`
- Dependencies: current PocketRisu asset/blob/thumbnail ownership audit; Android/Capacitor capability boundary; cache fingerprint/invalidation contract; physical mobile-memory measurement
- Priority: `P1`
- Lifecycle: `DESIGN_NEEDED`
- Source evidence: Haejeok adds a `NativeImage` Capacitor plugin that sampled-decodes bounded dimensions, writes derived WebP thumbnails to app cache, returns a local file path rather than transferring image bytes as JS Base64, limits decode concurrency, coalesces equal in-flight loads, keys thumbnails with size plus source metadata, and prunes the thumbnail cache. The same forward range reduces Android in-memory asset-cache budgets and throttles import progress updates.
- PocketRisu benefit: reduce Java/WebView/JS copying and peak decode pressure when large character/media grids request many thumbnails concurrently; create a disposable derived-cache path rather than retaining full source images for thumbnail display.
- Main risk/conflict: stale or wrong thumbnails if source fingerprinting is insufficient; duplicate cache ownership can leak memory/disk; Android-only behavior can diverge from web/Tauri; source file layout is not PocketRisu authority.
- Validation need: first map PocketRisu current image/asset path and prove the duplicate-copy/decode bottleneck; then measure peak memory/decode latency on representative Android grids, test same-key asset replacement invalidation, concurrent request coalescing, cache pruning, activity/plugin teardown, and fallback parity.
- Follow-up: assistant-owned design draft created at `hanmiyoo10-alt/-:products/pocketrisu-helper-mod/docs/features/mobile/native-thumbnail-cache/DESIGN.md`, commit `6df9017587ba738a7a6cc2583a3007f66997f725`. Remains design-only; no feature branch/PR opened.

This is a **deduplicated strengthening/refinement** of the existing `Character image thumbnail/preload optimization with bounded batch loading` ledger idea, not a separate competing architecture. The durable design narrows the first useful slice to derived thumbnails only and explicitly excludes backup/import/generation-lifecycle work.

### 2. TripleHwang/RisuVault v0.3.2.0 — durable per-chat message counts to avoid startup aggregation

- Source: `TripleHwang/RisuVault:main`
- Key commit: `ce4e448c36874ef1c511431b2611773e9002414e`
- Merge HEAD: `acabebafd1d295bede3093e2cf28ebfaab0cfdc1`
- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `MEDIUM`
- Size: `S`
- Evidence: `MEDIUM`
- Risk: `MEDIUM`
- Dependencies: PocketRisu relational SQLite/bootstrap query-plan audit; complete message mutation-path inventory; migration/backfill cost measurement; counter reconciliation contract
- Priority: `P1`
- Lifecycle: `DESIGN_NEEDED`
- Source evidence: RisuVault adds `chat_message_counts`, maintains counts transactionally with SQLite triggers for insert/delete/chat-move, backfills only chats missing counters, and changes metadata bootstrap from `COUNT(messages)` aggregation to a join against the durable counter table. Release notes state this avoids full message aggregation during `Opening SQL Database...` while preserving partial message loading.
- PocketRisu benefit: if PocketRisu still performs message-table aggregation during metadata/bootstrap, startup cost can become proportional to chats rather than all stored message rows, helping very large/long-chat databases.
- Main risk/conflict: a missed mutation path can create durable count drift; migration/backfill itself can cause startup work; PocketRisu may already have a different summary mechanism, making the idea unnecessary.
- Validation need: inspect the current PocketRisu schema/query plan first; measure the real aggregation cost; enumerate insert/delete/move/import/restore mutation paths; test transaction rollback, migration idempotency, deliberate counter corruption, reconciliation, and server-phone write overhead.
- Follow-up: assistant-owned design draft created at `hanmiyoo10-alt/-:products/pocketrisu-helper-mod/docs/features/server/durable-chat-message-counts/DESIGN.md`, commit `587fee505228a80f283783def2c4f5acdd90692e`. Storage/schema work remains design/investigation only under the autonomous gate.

This is merged as new concrete mechanism/evidence under the existing metadata-first startup / deferred hydration family; do not replace current PocketRisu save architecture merely because the source uses triggers.

### 3. HaejeokRisuai — Android foreground generation service is retained as a guardrail-conflicting reference

- Source: `nevaeh5379/HaejeokRisuai:main`
- Commit: `0d79e2a94bece24413e16b191040aa0438dda0d4`
- System impact: `SYSTEM_UPDATE_REQUIRED`
- Importance: `MEDIUM`
- Difficulty: `HIGH`
- Size: `M`
- Evidence: `MEDIUM`
- Risk: `MEDIUM`
- Dependencies: explicit client-device-only product direction; Android native lifecycle/service/permission review; separate notification policy; physical background-generation tests
- Priority: `P3`
- Lifecycle: `HOLD`
- Source evidence: Haejeok adds Android manifest foreground-service/wake-lock/notification permissions, a generation foreground service, persistent progress notification, optional result notification, and native request lifecycle hooks.
- PocketRisu benefit: potentially keeps a native client generation request alive while the app is backgrounded.
- Main risk/conflict: this changes Android service/permission behavior and introduces notifications. It must **never** be applied to the PocketRisu server-phone path because the standing guardrail forbids Android notifications there; native wake-lock/service lifecycle also expands device-level blast radius.
- Validation need: none for implementation while HOLD. If revisited later, first prove a client-phone-only boundary and explicitly separate it from server-phone deployment.
- Follow-up: evidence/history only. No design dossier, implementation branch, or PR created.

## Evidence merged into existing ideas

### Haejeok Android backup / file I/O range

The forward range after `44e6c22a795924460ae447678442351d1aba82b3` also includes:

- `328c854b6874b5eb05d147554df4a1efbd570dee` — Android `StreamFileWriter` for chunked writes to user-selected destinations.
- `fea4f0c073a1d70f92ef12f91465b58a02182113` — native backup import/export with chunked reads, staging, explicit session cleanup and asset-key normalization.
- `aef7e7479a24dbc1f2adf95a40131de8dedd62ff` — throttled import progress and lower cache budgets.

These strengthen existing ledger families rather than creating duplicates:

- **Stream large asset/backup payloads and batch DB writes** — native file/session code provides more evidence for bounded chunking and avoiding giant JS buffers.
- **Backup restore asset-path normalization/validation** — `NativeBackupPlugin.normalizeAssetKey()` rejects empty, `.` and `..` path segments after slash normalization and forces restored assets beneath the asset namespace. This is merged into the existing high-risk restore-boundary design; it does not lower that idea's Risk because restore remains destructive.
- **Backup materialization / cold-lazy compatibility** — native backup work is supportive evidence only; prior compatibility/cold-storage commits remain the stronger semantic source.

No autonomous implementation is authorized from these commits because native backup/restore changes cross destructive recovery and platform boundaries.

### Haejeok Android/Postgres architecture

`04fac4458b518d7a1c20f521f273db9d811e8796` introduces Android platform work plus Postgres-backed message/search/statistics queries. This is retained as architecture evidence only and deduplicated into existing storage/offload ideas. A Postgres migration is not a quick-win candidate and remains outside autonomous implementation gates.

### TripleHwang/RisuVault v0.3.2.0 other changes

The release also contains asset MIME detection from magic bytes, long-chat upward-scroll refresh fixes while preserving bounded DOM/SQL partial loading, SafeElement event scoping, and product/UI features. These are useful supporting evidence but do not justify standalone duplicate backlog entries in this pass.

## Cursor changes

Forward review is complete through these current HEADs:

- `nevaeh5379/HaejeokRisuai`: `44e6c22a795924460ae447678442351d1aba82b3` -> `383677e5ce4c67f30d40791b579e131a62c3813c`
- `TripleHwang/RisuVault`: `f81043922db3af8e4cc4ff411a91f85a2169dfc0` -> `acabebafd1d295bede3093e2cf28ebfaab0cfdc1`

Rechecked with no newer meaningful commit in the combined active-source scan: `rpaddict/RisuBard`, `kwaroran/Risuai`, `kwaroran/Risuai-Next`, `PocketRisu/PocketRisu:develop`, `InoriNatsume/RisuVault`, `seto-sama/PocketRisu-Kei`, `Nagase-Kotono/PocketRisu-kotono`, `tegy1117/Kei-Risu`, `PocketRisu-Alter/PocketRisu-Alter`.

## Historical backfill

`HISTORICAL_BACKFILL_COMPLETE_THROUGH` does not advance. Forward traffic was non-trivial, and complete historical coverage through a common date is still not proven for all tracked sources.

## Autonomous progression result

- Ledger/handoff addendum: this file.
- Design dossier: `products/pocketrisu-helper-mod/docs/features/mobile/native-thumbnail-cache/DESIGN.md` in `hanmiyoo10-alt/-` @ `6df9017587ba738a7a6cc2583a3007f66997f725`.
- Design dossier: `products/pocketrisu-helper-mod/docs/features/server/durable-chat-message-counts/DESIGN.md` in `hanmiyoo10-alt/-` @ `587fee505228a80f283783def2c4f5acdd90692e`.
- Implementation branches/tests/personal source PRs: none; both executable candidates remain `DESIGN_NEEDED`, and the foreground-service idea is `HOLD`/`SYSTEM_UPDATE_REQUIRED`.
