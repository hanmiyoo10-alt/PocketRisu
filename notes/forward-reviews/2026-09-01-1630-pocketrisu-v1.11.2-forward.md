# PocketRisu forward review — 2026-09-01 16:30 KST

Source: `PocketRisu/PocketRisu:develop`

Authoritative prior cursor: `b8bbcbe065755379d33f74d6ad16a36d634917c1`
Reviewed current HEAD: `278251f85a19bfdfd4cf3faae780e62682878f9e`
Compare status: ahead by 14 commits; cursor only moves forward.

## Meaningful ideas

### PLUGIN-STORAGE-REFRESH-TOP-UP-ONLY

Source evidence:
- `127ca67fd6a11be37cb23a1b7c4598df14404144` — index refresh no longer re-runs the bulk `/api/plugin-storage/all` stream; refresh tops up only keys missing from cache.
- `167def7df98e8272dcb179a4e8b4451e29e32604` — values that fail to parse are remembered as unparseable so periodic refresh does not fetch the same permanently-corrupt row again; a later write or disappearance from the server index clears that suppression.

Classification:
- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `S`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: `NONE`
- Priority: `P0`
- lifecycle status: `ADOPTED`

Benefit: prevents periodic V2 key enumeration from repeatedly saturating remote links with the whole plugin store; keeps background index refresh proportional to actual missing keys and prevents a corrupt row from becoming a recurring network/CPU tax.

Conflict/risk: suppression must not permanently hide a repaired key. Write repair and server-index removal must clear the unparseable marker; cache/index must never claim a value the server did not durably accept.

Validation need: preserve regression coverage for bulk preload only once, top-up-only refresh, multi-refresh corrupt-row suppression, rewrite recovery, remote-device-added key discovery, and write ordering.

Follow-up: preserve as a PocketRisu storage invariant. Do not regress to whole-store background re-streaming when V2 plugin enumeration or diagnostics evolve.

### SELF-UPDATE-PRESERVES-CUSTOM-BACKUP-OWNERSHIP

Source evidence:
- `32a120ed2776f9adb91d93c77b74892f16878094` hotfix merge includes server backup/update hardening: stale `risu-backup-<ts>.bin.tmp` sweep after a bounded stale age, backup success reports its actual directory, and self-update adds a custom app-root backup top-level directory to the keep set unless it is already a managed/default backup root.

Classification:
- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `S`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: `NONE`
- Priority: `P0`
- lifecycle status: `ADOPTED`

Benefit: prevents self-update cleanup from deleting an operator-selected backup directory inside the app root, while also cleaning abandoned temp backup artifacts without touching fresh/in-flight temp files.

Conflict/risk: update keep rules are data-loss sensitive. Path classification must distinguish default/managed roots from custom top-level data, and temp cleanup must remain stale-only and pattern-bounded.

Validation need: test custom backup directory inside app root, default/external backup roots, managed-root exclusions, update preservation, interrupted backup leaving `.tmp`, fresh `.tmp` survival, stale `.tmp` cleanup, and path traversal/absolute-path cases.

Follow-up: preserve as an updater/backup ownership invariant; do not broaden update deletion rules without matching backup-path tests.

## Other reviewed forward signals

- `f39114932c383da430c5d58d4a83366d107dbd98` fixes message first-paint latency by resolving asset names locally from prefetched manifests and keeping server resolve as cold-cache fallback. This reinforces the existing bounded/local-first manifest/cache performance direction; no separate autonomous implementation candidate was created in this pass.
- `a848077383f51c3a96c8210f4da09df256337cee` is toast swipe UX/library wiring and is low-impact polish, not a pipeline-priority idea.
- `278251f85a19bfdfd4cf3faae780e62682878f9e` is the v1.11.2 version bump.

## Progression result

Both high-signal items are already `ADOPTED` upstream, so no feature branch, production code change, test execution, or personal-fork PR was created. Durable ledger/invariant records were added and the official PocketRisu cursor may advance to `278251f85a19bfdfd4cf3faae780e62682878f9e`.

`HISTORICAL_BACKFILL_COMPLETE_THROUGH` is unchanged because this was forward review, not complete historical coverage.