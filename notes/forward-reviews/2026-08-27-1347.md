# Forward review — 2026-08-27 13:47 KST

Reviewed only commits newer than the authoritative forward cursors.

## rpaddict/RisuBard

- Previous cursor: `3ae4501bf40fa5bb86bb20a93c4f4d7cf4b48a93`
- Reviewed through: `372b4efe0cb8a10d40f4e227ad949d78b8aadc60`
- Range: 9 commits.

### Deduplicated idea update — side-effect-aware validated model-response boundary

This strengthens the existing bounded retry/fallback request-safety family rather than creating an unrelated retry idea.

- **System impact:** `NO_SYSTEM_UPDATE`
- **Importance:** `HIGH`
- **Difficulty:** `LOW`
- **Size:** `S`
- **Evidence:** `MEDIUM`
- **Risk:** `MEDIUM`
- **Dependencies:** audit current PocketRisu provider response metadata and retry/tool-execution ownership; identify which structured-output callers persist or execute tools before validation
- **Priority:** `P0`
- **Lifecycle:** `DESIGN_NEEDED`
- **Source evidence:** `rpaddict/RisuBard` release `372b4efe0cb8a10d40f4e227ad949d78b8aadc60`; `packages/risubard-core/src/modelResponse.ts`; v0.9.1 notes. The implementation propagates finish reasons, distinguishes blocked/truncated/empty/invalid structured output, caps validation retries at two attempts, and marks retries non-retryable after tool execution or caller-declared `noRetry`.
- **Benefit:** prevents truncated/provider-blocked structured output from being accepted as valid while avoiding duplicate side effects from blindly retrying requests that already executed tools or writes.
- **Conflict/risk:** provider finish-reason taxonomies differ; moving retry ownership can alter existing provider-specific fallback behavior; retrying after partial streaming/tool execution can duplicate effects.
- **Validation need:** map PocketRisu finish/stop metadata end-to-end; tests for length/content-filter/refusal/empty/invalid structure; prove at-most-one retry for pure generation and zero automatic replay after tool execution/write side effects; preserve cancellation/auth failures as non-format failures.
- **Follow-up:** assistant-owned design dossier created in helper repo; remain `DESIGN_NEEDED` until PocketRisu request-path ownership is audited. Do not implement from RisuBard wholesale.

Additional evidence merged into existing ideas:

- Bounded retry/fallback: v0.9.1 explicitly prevents nested client/server retries and does not restart a request after tool execution or already-emitted streaming output.
- Streaming interruption safety: preserve the last received partial text for display but do not mark it complete or trigger downstream automatic memory/wiki analysis.
- Staged/atomic operations: chat merge writes a new chat without mutating originals, moves only after save succeeds, and refuses ambiguous duplicate-message-ID auto-deduplication. This is evidence for existing staging/fail-safe mutation patterns, not a new PocketRisu chat-merge proposal.

## kwaroran/Risuai-Next

- Previous cursor: `8dae43c533e5d4a1211298d1f8a6400266c94f74`
- Reviewed through: `b0d40f89a9f40b29900d86e5251a78649b2c6173`
- Range: 1 commit (`feat: basic database and formatting`).

The commit introduces an early database abstraction supporting Node SQLite, better-sqlite3, libSQL, Bun SQLite, and Cloudflare D1. This is meaningful project divergence, but it is an architecture seed rather than a bounded PocketRisu improvement. PocketRisu already has established persistence/server-phone guardrails, so no new port candidate was created. Keep Risuai-Next active as an architectural evidence source and watch for later concrete transaction/migration/compatibility lessons.

## Historical backfill

No global `HISTORICAL_BACKFILL_COMPLETE_THROUGH` change. This run processed forward traffic; no active cursor was moved backward and no claim of complete historical coverage was made.
