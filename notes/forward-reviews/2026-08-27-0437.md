# Forward review — 2026-08-27 04:37 KST

## Source cursor movement

- Source: `nevaeh5379/HaejeokRisuai`
- Previous `Last reviewed HEAD`: `383677e5ce4c67f30d40791b579e131a62c3813c`
- Reviewed through: `0c08e3974a6745a1a50ea86db55727f0d89b4d76`
- Cursor direction: forward only

New commits reviewed in this bounded pass:

- `9b7db4fd84bfe27b19c1579f5acedd963d8ac17d` — mobile settings/Vite worktree dev fix; no distinct PocketRisu pipeline candidate promoted.
- `81dcbe07bf1959262fa68969990f6b7bf0a7cccc` — relational node codec changed the default flatten row budget from `250_000` to `Infinity`.
- `8636fae09e9e468b62ba142e593860e8bf7785ba` — prompt-settings UI affordances; useful UX reference, not promoted at current priority.
- `0c08e3974a6745a1a50ea86db55727f0d89b4d76` — prompt item/author-note layout; useful UX reference, not promoted at current priority.

## Normalized idea / regression lesson

### Caller-owned bounds for relational/tree flattening

- `System impact`: `NO_SYSTEM_UPDATE`
- `Importance`: `HIGH`
- `Difficulty`: `MEDIUM`
- `Size`: `S`
- `Evidence`: `MEDIUM`
- `Risk`: `MEDIUM`
- `Dependencies`: PocketRisu has no current `relationalNodeCodec`/`flattenRelationalValue` equivalent on the inspected branch; only relevant if a future relational/tree serializer or migration introduces a comparable flattening boundary.
- `Priority`: `P1`
- lifecycle status: `HOLD`
- source evidence: `nevaeh5379/HaejeokRisuai` `81dcbe07bf1959262fa68969990f6b7bf0a7cccc`
- benefit: preserve an explicit resource budget at recursive/relational serialization boundaries so malformed or unexpectedly large state cannot silently turn a save/migration operation into unbounded row materialization.
- conflict/risk: a single hard-coded global cap can reject legitimate large datasets, while a default of `Infinity` removes the survival guard entirely. The transferable principle is **caller-owned, operation-specific budgets with explicit failure semantics**, not copying either constant.
- validation need: if PocketRisu later introduces a comparable codec, test normal large datasets near the accepted ceiling, deterministic over-budget failure, deep nesting, cyclic inputs, and migration/backup behavior without partial publish.
- follow-up: keep as architecture regression knowledge. Re-evaluate only when a PocketRisu serializer/migration has this shape; then design separate trusted-migration vs interactive/untrusted-input limits rather than an unconditional global default.

## Deduplication / progression decision

This is stored as a regression/invariant lesson rather than a new implementation queue item. It does not justify adding the Haejeok relational storage architecture to PocketRisu and does not pass the autonomous implementation gate because there is no matching PocketRisu ownership boundary to modify.

No design dossier, implementation branch, tests, or PR were created in this pass.

## Historical backfill

No `HISTORICAL_BACKFILL_COMPLETE_THROUGH` change. This was a forward-traffic pass; complete common historical coverage across all tracked sources is still not proven.
