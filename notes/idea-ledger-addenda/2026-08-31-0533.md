# Idea ledger addendum — 2026-08-31 05:33 KST

## PLUGIN-PERSIST-BEFORE-RUNTIME-RELOAD

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P0`
- Lifecycle status: `READY_TO_PORT`
- Source evidence: `nevaeh5379/Risuai@3b5b3d39425a6297e8ea8a634e6d957e17c7b771`; direct inspection of `hanmiyoo10-alt/PocketRisu:develop` `src/ts/plugins/plugins.svelte.ts` and `src/ts/globalApi.svelte.ts`.
- Benefit: make plugin import/update durability happen-before runtime reload so an updated plugin cannot appear to work in-session while its persisted state is still pending and later revert.
- Conflict/risk: ordering-only change must preserve targeted V3 reload, existing save/integrity optimizations, and all PocketRisu DB guardrails. If persistence cannot complete, runtime reload must not be reported as a durable update success.
- Validation need: regression tests for both V3 targeted reload and non-V3/general reload ordering; save failure path; type/project checks and diff check.
- Follow-up: implement as one isolated feature branch/PR when a clean checkout can run verification. Current automation runtime is blocked by GitHub DNS for local git/test setup; no unverified code write was performed.

### Deduplication / normalization note

This normalizes the older backlog row `Persist plugin updates before targeted runtime reload` rather than creating a second underlying idea. The old source evidence and lifecycle history are preserved. Direct PocketRisu inspection raises confidence to `Evidence: HIGH` and establishes the current minimal XS slice (`await requestImmediateSave()` before retaining the existing targeted/general reload branch).
