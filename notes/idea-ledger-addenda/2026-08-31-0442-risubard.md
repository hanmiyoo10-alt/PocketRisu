# Idea ledger addendum — 2026-08-31 04:42 KST

## CHAT-READING-POSITION-ANCHOR-RESTORE

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `MEDIUM`
- Size: `S`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: stable chat/message render identity; current PocketRisu chat-container/window ownership audit; compatibility contract with `PROGRAMMATIC-SCROLL-ORIGIN`
- Priority: `P1`
- lifecycle status: `DESIGN_NEEDED`
- source evidence: `rpaddict/RisuBard@f1ad9f75407e48e8053908ad7ab58fa94ff5faf6`; `src/lib/ChatScreens/chatScrollAnchor.ts`, `chatScrollAnchor.test.ts`, `ChatScrollAnchorIntegration.test.ts`, and `DefaultChatScreen.svelte` integration. Tests cover top-visible message/offset capture, container-local correction, context/missing/stable early exits, latest-message append bypass, bounded layout stabilization wiring, and the preservation setting.
- benefit: preserve the reader's logical location when images, markdown, child-list changes, or rerenders above the viewport alter layout; avoids forcing a history reader to manually find the same message again.
- conflict/risk: restoration can fight live streaming/autoscroll, pagination/window changes, direct user scroll, edits/reordering/deletion, or stale message identities. A numeric render index cannot be treated as durable cross-context identity without explicit context ownership.
- validation need: reproduce async image-height and markdown/layout shifts above the viewport; verify direct user scroll cancels correction; verify latest-message append remains owned by normal live-scroll behavior; verify character/chat switch, page/window change, deleted/reordered anchor, folded messages, and programmatic-scroll arbitration; ensure corrections are bounded and chat-container-local.
- follow-up: use the helper design dossier to audit the personal fork's current chat container and scroll owners. Resolve canonical anchor identity and interaction with programmatic-scroll ownership before moving to `READY_TO_PORT`. No implementation branch until those assumptions are closed.

Deduplication note: this is intentionally distinct from `PROGRAMMATIC-SCROLL-ORIGIN`. That feature decides whether automatic scrolling may take ownership after user interaction; this feature repairs layout drift while preserving an already user-owned reading position. The contracts must compose.

## Existing bounded request retry/fallback state machine — evidence merge

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `S`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: current PocketRisu provider/cancel/tool-call retry ownership audit
- Priority: `P0`
- lifecycle status: `READY_TO_PORT`
- source evidence: retain prior `nevaeh5379/HaejeokRisuai@807f33b1` and `f3fb11d2`; merge `rpaddict/RisuBard@f1ad9f75407e48e8053908ad7ab58fa94ff5faf6`, whose tests distinguish retryable thrown network/internal-timeout/socket failures from explicit user abort and route safe thrown transport failures through the shared bounded loop while avoiding the tool-bearing path.
- benefit: makes transport failure handling converge on one bounded retry budget instead of leaving thrown fetch/socket failures outside policy or accidentally retrying user cancellation.
- conflict/risk: provider adapters encode different cancellation and idempotency semantics; retrying after a side effect or tool call can duplicate work. `AbortError` alone is not sufficient to distinguish timeout from user cancellation without ownership context.
- validation need: inventory current PocketRisu thrown network/timeout/cancel paths; hard-bound attempts; prove explicit cancellation exits immediately; prove tool/side-effect requests are not replayed by this generic path; preserve existing provider-specific fallback semantics.
- follow-up: no duplicate idea created. Before autonomous code progression, re-audit the current personal-fork request loop and split an isolated classification-only slice only if the owner and regression tests are unambiguous.