# Idea ledger addendum — 2026-09-02 02:32 KST

## PLUGIN-STORAGE-READ-YOUR-WRITES

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: `NONE`
- Priority: `P0`
- lifecycle status: `ADOPTED`
- Source evidence: `PocketRisu/PocketRisu@83ffa0474abd013581c4df23e50b20c559d4b47a` (`fix(plugin storage): read back a pending async write before it lands on the server`).
- PocketRisu benefit: preserves synchronous plugin-storage compatibility after moving persistence behind an asynchronous server boundary. Immediate `getItem()` and `snapshotAll()` observe the accepted pending mutation instead of an older cache/server copy.
- Main conflict/risk: pending local state has only temporary per-key observation authority. It must be cleared/resolved correctly, respect mutation ordering, and cooperate with rollback-generation protection so an older failure cannot overwrite a newer mutation.
- Validation need: keep focused held-in-flight tests for set→get, set→snapshot and remove→get; add/retain coverage for queued writes, clear, write failure and rollback-generation interaction whenever this subsystem changes.
- Follow-up: preserve as an invariant in the helper dossier. No implementation branch/PR: the behavior and focused regressions are already adopted in official PocketRisu.

### Deduplication note

Keep separate from `OPTIMISTIC-CACHE-ROLLBACK-USES-WRITE-GENERATION`. That invariant controls rollback authority after asynchronous write failure; this one controls read visibility before asynchronous persistence completes. Evidence should be cross-linked, not merged into one lifecycle item.