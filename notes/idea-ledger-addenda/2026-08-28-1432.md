# Idea ledger addendum — 2026-08-28 14:32 KST

## Bounded module asset editor / preview work

This addendum normalizes and strengthens the existing idea `storage UI/perf — paginate large asset/file lists; fetch thumbnails only for active page; dedupe in-flight thumbnail requests` rather than creating a duplicate.

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `S`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P0`
- Lifecycle status: `READY_TO_PORT`
- Source evidence: `nevaeh5379/Risuai@58b980ca`; independent module-specific confirmation in `rpaddict/RisuBard@769c611cc3574e6b0277e944afa1ffaaf99c100d`
- PocketRisu applicability evidence: personal fork `main` currently renders all `currentModule.assets`, eagerly resolves every preview through `getFileSrc()` when previews are enabled, and has no visible-row bound in `src/lib/Setting/Pages/Module/ModuleMenu.svelte`.
- Benefit: prevents large module asset sets from turning the settings UI into an unbounded DOM/media-preview workload; improves mobile responsiveness and lowers memory/decode/network pressure.
- Conflict/risk: pagination must preserve edits/deletes against the correct underlying asset indexes; preview cache/in-flight ownership must not show stale media or leak object URLs; the existing separate Asset Viewer must remain unchanged.
- Validation need: focused regression proving only a bounded page is rendered and only visible previews are resolved, pagination/load-more preserves edits/deletes, media is lazy/preload-bounded, plus project type/check validation.
- Follow-up: implement as one isolated feature branch/PR once a clean executable checkout is available. Do not mix importer refactors into the same PR.

### Related import evidence

The same RisuBard commit reduces legacy module decode/persist groups to mobile-safe worker-sized batches. This supports the existing large-payload/batched-I/O design family, but PocketRisu's current `readModule()` has a different task/concurrency implementation. Treat it as evidence only until peak-memory behavior is measured; do not copy the source batch constant blindly.

### Execution note

A clean source checkout could not be established in this run because `github.com` DNS resolution failed from the executable environment. GitHub API access remained healthy. This is an integration/execution blocker, not a code or CI failure, so no source modification or draft PR was created.