# Idea ledger addendum — 2026-09-01 19:37 KST

## PLUGIN-STORAGE-PARTIAL-WRITE-MERGE-SEMANTICS

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: `NONE`
- Priority: `P0`
- Lifecycle status: `ADOPTED`
- Source evidence: `PocketRisu/PocketRisu` `ebe32742a22b123eb0c52e4dc387d641090dee8a` (`fix(plugin-storage): merge instead of replace on pluginCustomStorage writes`). The commit documents a real dogfood loss sequence and adds empty/partial round-trip regression tests.
- Benefit: prevents unrelated plugin data from being erased when generic DB APIs round-trip an incomplete compatibility view after plugin storage has been externalized.
- Conflict/risk: omission becomes non-destructive by design. True replacement must remain explicit; do not apply merge semantics to a contract that proves it carries a complete authoritative snapshot.
- Validation need: keep empty round-trip, partial write preserving unrelated keys, and explicit clear behavior covered. Future migrations must identify whether incoming state is complete before allowing replacement.
- Follow-up: preserve this invariant in plugin-storage API/migration reviews. Already adopted in official PocketRisu; no branch/PR needed.

Deduplication: separate from refresh top-up, optimistic rollback generation, and lazy-manifest provenance. This item owns the distinction between **partial compatibility-view writes** and **authoritative destructive replacement**.
