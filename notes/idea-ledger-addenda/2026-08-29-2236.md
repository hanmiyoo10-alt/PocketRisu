# Idea ledger addendum — 2026-08-29 22:36 KST

## AUXILIARY-MODEL-JOB-CANCELLATION-OWNERSHIP

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `MEDIUM`
- Risk: `LOW`
- Dependencies: matching PocketRisu-owned long-running auxiliary-model job with explicit retry/publish or resumable lifecycle
- Priority: `P1`
- Lifecycle status: `HOLD`
- Source evidence: `rpaddict/RisuBard` `c1259e98bbb6b27697e2e891a96b5cb230c5bff8`
- Benefit: lets users stop expensive or stuck auxiliary model work promptly, prevents post-cancel retries/publication, and preserves resumable work without misclassifying deliberate cancellation as failure.
- Conflict/risk: cancellation ownership can be wired too broadly and abort unrelated primary chat generation; stopping only the visible task while retries/finalizers continue would create false cancellation semantics.
- Validation need: identify a PocketRisu-owned auxiliary job with a reproducible non-cancellable path; add tests proving cancellation reaches the provider, prevents retry/finalization, leaves unrelated chat generation untouched, and maps resumable jobs to paused/cancelled rather than failed.
- Follow-up: keep as an invariant until a matching owner exists; if one appears, first slice is a failing lifecycle test plus one scoped `AbortSignal` propagation path.

Deduplication note: this is not a duplicate of bounded retry/fallback. Retry bounding caps autonomous attempts; this item governs explicit user cancellation ownership across request, retry, publish, and resumable-state boundaries.