# Idea ledger addendum — 2026-09-01 12:33 KST

## REQUEST-WIRE-MODEL-OBSERVABILITY

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P1`
- lifecycle status: `ADOPTED`
- source evidence: official `PocketRisu/PocketRisu` commit `764c62c903f0cc2d4d276dba2a3ec228733664e4` resolves `resolveWireModelId(preset)` and propagates the effective wire model through request logs and request result metadata instead of relying on preset display name or incomplete snapshot model fields.
- benefit: makes diagnostics and generation metadata reflect the provider-facing model actually selected, especially for renamed presets and profiles whose model comes from user values.
- conflict/risk: model observability must remain metadata-only; do not broaden logging to credentials, auth headers, or prompt/request bodies. Resolution failure fallback is only for identifying an already-failed configuration.
- validation need: cover user-value model IDs, renamed presets, preview, success, streaming, failure, and broken-resolution fallback; verify no secret material is logged.
- follow-up: preserve a single authoritative effective-model resolver for future adapters/request surfaces and reuse it for observability rather than reconstructing identity from UI labels.

Deduplication note: the same commit also fixes a stale model-settings route after settings relocation. That portion overlaps existing settings deep-link/routing ownership invariants and is not registered as a second underlying idea.

Progression: `ADOPTED`; durable review/dossier only. No implementation branch, code change, test run, or personal PR created.