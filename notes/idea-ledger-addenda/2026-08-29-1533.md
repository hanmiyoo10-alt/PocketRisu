# Idea ledger addendum — 2026-08-29 15:33 KST

## IMAGE-GENERATION-PROVENANCE-OWNERSHIP

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `LOW`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `MEDIUM`
- Risk: `LOW`
- Dependencies: matching PocketRisu-owned durable generated-image/inlay path plus demonstrated provenance loss or inconsistent metadata across multiple entry points
- Priority: `P2`
- Lifecycle status: `HOLD`
- Source evidence: `seto-sama/PocketRisu-Kei` commit `016fad84a24db574ea1dff9caedf48a3157ce735`
- Benefit: keep generated-image provenance consistent across multiple creation entry points by attaching metadata at the durable artifact owner rather than in each caller
- Conflict/risk: source-specific inlay/schema fields must not be copied without a PocketRisu owner; provenance is descriptive and must never be treated as an authorization boundary
- Validation need: inventory PocketRisu generated-image persistence paths; reproduce an entry-point metadata mismatch; verify explicit-target and current-context fallback semantics; add parity tests across all owned generation entry points
- Follow-up: `HOLD` until a matching PocketRisu owner and concrete mismatch exist; if triggered, start with one failing regression test and one canonical artifact-owner helper rather than caller-by-caller patches

### Deduplication note

This is distinct from asset-list pagination, blob URL ownership, export round-trip compatibility, and required-asset completeness. It concerns provenance attachment ownership for newly generated durable image artifacts. Merge future evidence here rather than creating per-provider or per-entry-point duplicates.
