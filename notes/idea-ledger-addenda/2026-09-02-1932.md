# Risu-family idea ledger addendum — 2026-09-02 19:32 KST

## SORTABLE-FILTERED-CONTROLS-REMAIN-INTERACTIVE

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P1`
- lifecycle status: `ADOPTED`
- source evidence: official `PocketRisu/PocketRisu` commit `dd07f4d10c3d11b5194f58246246ba04ef2903dd`; current `develop` still retains `preventOnFilter: false` next to `.no-sort` in shared `sortableOptions`.
- benefit: keeps plugin argument inputs and toggles usable inside drag-excluded regions, including avoiding iOS synthetic-click cancellation, while still preventing those regions from acting as drag handles.
- conflict/risk: drag filtering and control-event suppression are different ownership concerns. A future change that restores SortableJS's default `preventOnFilter` globally can regress form focus/toggle activation; a future surface needing suppression should scope it locally.
- validation need: regression test or interaction harness proving `.no-sort` inputs can focus, toggles activate, and drag does not start from the filtered region across mouse/pointer and relevant touch/iOS behavior.
- follow-up: preserve as a shared UI invariant during SortableJS upgrades/refactors; do not create a new implementation PR while current official `develop` already satisfies it.

Deduplication note: no existing ledger/backfill entry was found for `preventOnFilter`, `.no-sort` interaction ownership, or the underlying SortableJS filtered-control event contract. This remains separate from responsive-shell breakpoint state and plugin persistence/update invariants.

Historical coverage marker: unchanged. This addendum records one bounded official-PocketRisu historical slice and does not establish complete all-source coverage through a date.
