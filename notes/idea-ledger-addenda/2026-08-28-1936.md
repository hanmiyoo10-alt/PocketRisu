# Idea ledger addendum — 2026-08-28 19:36 KST

## HOLD — detached module editor requires explicit draft/live ownership

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `MEDIUM`
- Size: `S`
- Evidence: `MEDIUM`
- Risk: `MEDIUM`
- Dependencies: direct PocketRisu module-edit profiling plus explicit ownership/merge semantics for concurrent module changes and every editor exit path
- Priority: `P2`
- Lifecycle status: `HOLD`
- Source evidence: `PocketRisu-Alter/PocketRisu-Alter` `219dc859ed6892b292196e42ff4edef425f10d05`, reverted by `855144c732c5a09201fd14e10e822aabdbf6f04e`
- Benefit: avoid broad reactive/save work on every keystroke when editing very large modules, if profiling confirms that the current live-bound editor is a real hot path.
- Conflict/risk: detached snapshots change ownership and commit semantics; stale drafts can overwrite concurrent live/plugin changes, and implicit unmount commits can save on unrelated navigation. The source implementation was immediately reverted and therefore cannot be treated as validated architecture.
- Validation need: measure current large-module typing cost first; then test Save, Cancel/navigation/unmount, reorder/delete while editing, concurrent external/plugin mutation, stale-draft detection, merge/reject behavior, and crash/reload behavior.
- Follow-up: preserve the invariant that performance isolation must not silently change persistence ownership. Reconsider only after profiling proves the hot path and a conflict-safe draft contract exists.

Deduplication note: this is a regression/ownership lesson, not a new READY_TO_PORT optimization. It should be merged with any future module-editor performance work rather than duplicated as a detached-copy feature.
