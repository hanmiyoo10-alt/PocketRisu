# Idea ledger addendum — 2026-08-30 21:48 KST

## CROSS-REALM-BLOBLIKE-IMPORT-BOUNDARY
- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P1`
- Lifecycle: `READY_TO_PORT`
- Source evidence: `nevaeh5379/HaejeokRisuai@d6f30d8e5c8aaf8add101aab75a09b1fac502a48`
- PocketRisu benefit: support Android/content-provider/cross-realm File-like CharX inputs without falling back to unreliable `File.stream()`, while retaining bounded slicing.
- Main conflict/risk: use a narrow capability predicate (`safe integer size >= 0` + callable `slice`) and preserve explicit handling for Uint8Array/ReadableStream; reject unsupported objects rather than guessing.
- Validation need: cross-realm wrapper, normal File, Uint8Array, unsupported-object rejection, bounded slice size and asset import regressions.
- Follow-up: matching personal-fork `src/ts/process/processzip.ts` still uses `instanceof File` followed by `data.stream()`. Candidate is implementation-ready by design, but production modification is blocked until a clean checkout and focused tests can be run; current runtime fails DNS resolution for `github.com`.

Durable review: `notes/forward-reviews/2026-08-30-2148-haejeok-cross-realm-charx.md`.
