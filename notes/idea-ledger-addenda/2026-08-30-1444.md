# Idea ledger addendum — 2026-08-30 14:44 KST

## TOKEN-COUNT-ASYNC-OBSERVER-BOUNDARY — implementation-boundary revalidation

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `MEDIUM`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P1`
- lifecycle status: `READY_TO_PORT`
- source evidence: `seto-sama/PocketRisu-Kei@ae05122d6f7423a285e74d1e07fe245ac1847236` centralizes the existing CBS-aware token-count observer into `src/lib/UI/GUI/TokenCount.svelte`; personal fork branch `feat/token-count-async-observer-boundary@e57c0435018646800566f2158fd1a9fa12caa9e2` was re-read successfully and confirms the matching owner at `src/lib/SideBars/CharConfig.svelte`: three independent 400 ms timer/sequence observers for description, first message, and active-chat author note.
- benefit: the previous uncertainty about the matching PocketRisu path/owner is resolved; the feature remains a behavior-preserving extraction with a concrete one-owner scope.
- conflict/risk: do not land an unused helper-only slice. The existing dossier acceptance requires replacing all three inline observers in the same isolated feature PR, preserving `tokenizeAccurate()`, the 400 ms debounce, immediate generation invalidation, stale-result rejection, source values, styling intent, and all PocketRisu persistence/runtime guardrails.
- validation need: `pnpm check` plus focused build/tests or equivalent CI must succeed after the complete extraction; verify `CharConfig.svelte` has no remaining local token timer/sequence machinery and visible counts still bind to the same three values. The repository PR workflow runs check/build/tests only for PRs targeting `main`, while this feature branch is correctly based on `develop`, so opening a `develop` PR is not itself a substitute for pre-PR verification.
- follow-up: implementation remains tooling-blocked, but the blocker is now narrowed. GitHub connector can read the large `CharConfig.svelte` and confirms its exact contents/path, yet mutation is whole-file replacement; the local execution environment still cannot resolve `github.com`, so there is no safe clean clone/patch/`pnpm check` path. Do not guess or replace a truncated large file, and do not commit only an unused `TokenCount.svelte`. Resume when a full-file-safe patch/test path is available; then perform the dossier's single-PR extraction and open a personal-fork draft PR only after verification.

Branch verification: `feat/token-count-async-observer-boundary` still points exactly to base `e57c0435018646800566f2158fd1a9fa12caa9e2`; no production commit, test result, or PR exists yet. This is an integration/tooling blocker, not a code/CI failure.

Forward note: bounded checks observed `rpaddict/RisuBard` still at `c1259e98bbb6b27697e2e891a96b5cb230c5bff8` and `kwaroran/Risuai` still at `984f46b7306ca38312a043e0ef28d447f2a92766`; no cursor movement from those checks. `HISTORICAL_BACKFILL_COMPLETE_THROUGH` unchanged.
