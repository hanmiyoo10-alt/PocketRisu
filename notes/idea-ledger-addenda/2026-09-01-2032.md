# Idea ledger addendum — 2026-09-01 20:32 KST

## PROJECTED-RECORD-WRITES-PRESERVE-UNOWNED-METADATA

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: `NONE`
- Priority: `P0`
- Lifecycle status: `ADOPTED`
- Source evidence: `PocketRisu/PocketRisu` `86f2fe41f109662438e4686bffcf190f67de4741` (`fix: preserve message metadata in setFullChat`). The diff shows `setFullChat` previously reconstructed messages from only `role`/`data`, then changed to preserve prior message fields at the corresponding position before applying the script-owned content fields.
- Benefit: prevents scripting/compatibility operations that expose a narrow message projection from silently erasing richer per-message metadata owned by other features or plugins.
- Conflict/risk: preserving by index is valid only for a positional rewrite contract. If a future API allows arbitrary reorder/insert/delete, index identity can attach metadata to the wrong logical message; that case needs explicit stable identity or operation semantics.
- Validation need: regression test a metadata-bearing message through `setFullChat`; verify script-provided `role`/`data` replace the intended content while unowned fields survive. Add explicit tests before supporting reorder/length-changing semantics.
- Follow-up: preserve the projected-write ownership invariant in scripting/chat compatibility reviews. Already adopted in official PocketRisu; no implementation branch/PR required.

Deduplication: this is related to partial plugin-storage merge semantics but remains a separate record. Plugin storage concerns incomplete keyspace writes to an external authoritative store; this item concerns **field-level ownership of richer records exposed through a narrow compatibility projection**.
