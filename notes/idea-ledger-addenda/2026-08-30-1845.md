# Idea ledger addendum — 2026-08-30 18:45 KST

## MCP-LISTEN-BEFORE-SEND-BOUNDARY

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P0`
- Lifecycle status: `ADOPTED`
- Source evidence: `PocketRisu/PocketRisu@9f099bb21b6bd3e31085214320c34376c190b35a`. The custom MCP transport fix registers the response listener before `send()` and removes an async Promise executor. Current `hanmiyoo10-alt/PocketRisu:develop` preserves the same ordering in `src/ts/process/mcp/mcplib.ts`.
- Benefit: avoids a fast-response race that can miss the response and leave an MCP request hanging; also avoids unhandled-rejection hazards specific to async Promise executors.
- Conflict/risk: this invariant does not itself solve transport timeout or send-error propagation. A future cleanup must not trade the ordering fix for broader error handling.
- Validation need: regression fixture with an immediate/synchronous response after send; assert listener is live before send and the request resolves. If send rejection handling is changed, assert it rejects/settles deliberately rather than becoming unhandled.
- Follow-up: preserve during MCP transport refactors. Treat timeout/error-rejection semantics as a separate candidate if evidence justifies it.
- Durable review: `notes/backfill-reviews/2026-08-30-1845-pocketrisu-mcp-listen-before-send.md`.

Historical evidence is older than the authoritative source cursor, so no forward cursor moves backward. `HISTORICAL_BACKFILL_COMPLETE_THROUGH` remains unchanged.
