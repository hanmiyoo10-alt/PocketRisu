# Idea ledger addendum — 2026-08-31 11:44 KST

## RELEASE-TAG-ASSET-IDENTITY

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `S`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P1`
- lifecycle status: `ADOPTED`
- source evidence: `nevaeh5379/HaejeokRisuai@dbda43876b9322ce8284c166c28f0c3801f099b8` verifies the published release retains the requested build tag and fails closed after a bounded repair attempt; `nevaeh5379/HaejeokRisuai@b1fd6f3d3151eb347592339227ede5b3410b9ec9` pins generated release-note asset URLs to that same build tag. Current PocketRisu `.github/workflows/release.yml` already derives release `tag_name` and name from the triggering `github.ref_name` and uploads artifacts in the same tag-triggered release job.
- benefit: keeps operator-requested build identity, GitHub release identity, download identity, and updater-visible artifact version aligned, avoiding silent wrong-build distribution.
- conflict/risk: do not copy Haejeok-specific repair/rewrite machinery unless PocketRisu later introduces an equivalent split release boundary; multiple independent tag authorities would be worse than the current single tag-derived path.
- validation need: future release pipeline changes should prove trigger tag, target commit, release tag, artifact names/links, and updater-visible version agree; mismatches must fail closed.
- follow-up: preserve the current tag-derived release ownership as an invariant; add explicit read-back/link tests only if release publication or generated download-link ownership becomes split or indirect.

### Deduplication decision

The Haejeok publication read-back fix and download-link tag pinning are merged into this single idea because both protect the same build-identity invariant. This is distinct from `RUNTIME-DEPENDENCY-CLOSURE-DRIFT-GUARD`, which controls which runtime dependencies enter a portable artifact rather than which release/build identity the artifact belongs to.
