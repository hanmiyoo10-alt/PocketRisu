# Idea ledger addendum — 2026-08-31 07:32 KST

## RUNTIME-DEPENDENCY-CLOSURE-DRIFT-GUARD

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `MEDIUM`
- Size: `M`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: `NONE` for the adopted official invariant; a personal-fork backport requires an explicit audit because `hanmiyoo10-alt/PocketRisu:develop` currently predates the source packaging work
- Priority: `P2`
- lifecycle status: `ADOPTED`
- source evidence: `PocketRisu/PocketRisu@01681eeb5995156b065b65c6c02741ced4a3553d`
- benefit: derive and ship only the Node server/updater runtime dependency closure for prebuilt portable/Docker artifacts, reducing dead frontend dependency weight while making dependency drift explicit
- conflict/risk: an incomplete dependency graph can ship a runtime that passes build but crashes after release; dynamic/non-literal imports, native/platform dependencies, and updater-only edges must never be silently ignored
- validation need: fail closed on unsupported dynamic edges; regenerate and compare canonical minimal package metadata; install with the generated frozen lock; boot the assembled trimmed artifact and verify a real HTTP response across the release platform matrix
- follow-up: preserve the derived-closure + drift-check + assembled-runtime smoke-test invariant in official PocketRisu. Do not backport automatically to the lagging personal fork because the change is `M` sized and crosses release/Docker/portable build ownership.

### Deduplication note

This is not the same underlying idea as suppressing `onnxruntime-node`'s CUDA postinstall. A package-specific exclusion is a local optimization; this record captures the reusable authority rule that runtime dependencies are derived from executable server entry points, pinned reproducibly, checked for drift, and proven by booting the assembled artifact.
