# Idea ledger addendum — 2026-08-28 05:48 KST

This addendum is part of the durable idea ledger and should be merged into `notes/external-risu-ideas.md` during a safe normalization pass without deleting prior history.

## IMPORT-ANDROID-FILE-SLICE-STREAM

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P0`
- Lifecycle status: `READY_TO_PORT`
- Source evidence: `nevaeh5379/HaejeokRisuai` commit `860280bcdd7e08c49a00a7391076de89d3c68e0e`
- PocketRisu evidence: `hanmiyoo10-alt/PocketRisu:main` `src/ts/process/processzip.ts` still called `File.stream()` directly for `CharXImporter` File inputs when inspected.
- Benefit: preserve bounded streaming while supporting Android WebView/content-provider-backed Files whose `stream()` is unavailable or unreliable.
- Conflict/risk: localized File ingestion only; off-by-one/EOF or zero-byte loop are the main risks. No persistent representation, DB flush, system package, plugin reload, notification, or service-manager changes.
- Validation need: Android-style File regression test where `stream()` throws; focused Vitest; `pnpm check`; verify diff remains importer+test only.
- Follow-up: helper Feature-ID dossier exists; personal-fork implementation branch `feat/android-safe-charx-file-stream` contains implementation commit `f69d4f695d6d88af069257cc6d68eaba608ea5e2` and test commit `607d9cdf251babdf783be6e90b8a13fef36cf9a4`. Draft PR is intentionally blocked until executable validation succeeds. Current automation runner failed before tests because DNS could not resolve `github.com`; this is not a code/CI failure.

## Evidence merges — no duplicate idea

- `8ebb91cfe2bddeeb5fe92c4151c64b3537e49071`: merge into existing restore-format-admission / backup-entry path-validation family. Shared entry classification, invalid path rejection, unsupported-extension draining, ignored-entry count visibility.
- `25abe86f7853ec172b3e6ae51fc43be1f4099bbb`: merge into existing bounded restore / streaming SQL replacement / partial-hydration persistence family. Releases encoded DB buffers, avoids full-input slice copies, applies root/entity/message commits in batches, fails closed on partially loaded entities.

`HISTORICAL_BACKFILL_COMPLETE_THROUGH` unchanged.
