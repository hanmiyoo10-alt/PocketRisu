# Idea ledger addendum — 2026-08-28 10:45 KST

## Reclassification: direct `/index.html` runtime identity

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: `NONE` for design/readiness; implementation requires a clean checkout and focused request-level test execution
- Priority: `P0`
- Lifecycle: `READY_TO_PORT`
- Source evidence: `TripleHwang/RisuVault` `7d23e41425555fab02d8ef7a5b06269520b6f9be`, reviewed through merge `e750a66f513bd4a4161df7833367058363d949ac`; direct PocketRisu inspection of `server/node/server.cjs` on personal-fork `main`
- Benefit: direct `/index.html` navigation cannot silently select browser-side/runtime-default semantics while `/` selects Node standalone semantics; both entry paths receive the same server-owned runtime flags.
- Conflict/risk: route-order changes can alter static caching/startup semantics if implemented too broadly. Keep the change scoped to the HTML entry point and leave `/assets` and other static/API behavior unchanged.
- Validation need: request-level parity tests for `/` and `/index.html`, exactly-one runtime injection, same `__PATCH_SYNC__`, representative static asset/file behavior, no API/auth regression.
- Follow-up: helper dossier `products/pocketrisu-helper-mod/docs/features/server/direct-index-runtime-identity/DESIGN.md`; implement one isolated personal-fork PR only after a clean checkout can run focused tests. No official upstream PR automatically.

### Why Evidence changed to HIGH / lifecycle changed to READY_TO_PORT

The earlier forward review left this as `Evidence MEDIUM / DESIGN_NEEDED` pending PocketRisu-specific reproduction. Current inspection resolved that dependency structurally: generic `express.static(dist, { index: false, maxAge: 0 })` is registered, while runtime globals are injected in an `app.get('/')` HTML responder. Therefore direct `/index.html` can be owned by generic static middleware rather than the injected responder. Combined with the external causal fix, this is sufficient evidence to finalize the minimal design and rollback.

### Autonomous progression / blocker

- durable ledger normalization: completed
- helper feature dossier: completed
- source branch / modification: not started
- focused tests: not run
- personal draft PR: not opened

A clean local checkout was attempted, but the execution environment failed DNS resolution for `github.com`. Treat this separately from code/CI status. Because post-modification verification could not be guaranteed, no source mutation was made in this pass.

## Forward / historical cursor state

Active-source forward cursors were not moved backward or changed in this pass. The current sampled forward scan found no newer authoritative HEAD than the registry values. Historical `PocketRisu-Kei` inspection continued into older 2026-06 / 2026-05 material, but no additional distinct idea was promoted after deduplication.

`HISTORICAL_BACKFILL_COMPLETE_THROUGH` remains unchanged; this pass does not prove complete reviewed coverage across every tracked source through a common date.
