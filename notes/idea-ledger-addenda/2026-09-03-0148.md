# Idea ledger addendum — 2026-09-03 01:48 KST

## PLUGIN-WRITEBACK-AUTHORITY-SURVIVES-LRU-EVICTION

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `MEDIUM`
- Dependencies: `NONE`
- Priority: `P0`
- lifecycle status: `ADOPTED`
- source evidence: `PocketRisu/PocketRisu@2981235e49135b7e65849569a659e6954c91190d`; current `develop@278251f85a19bfdfd4cf3faae780e62682878f9e` retains handed-out fingerprint matching.
- benefit: keeps plugin snapshot write-back semantics stable when the small full-manifest LRU evicts an item that was already handed to a plugin; unchanged hydrated arrays still collapse back to their durable manifest descriptor instead of being accidentally re-inlined.
- conflict/risk: the handoff record itself must remain bounded and revision-safe enough not to misclassify real edits; cache state must remain performance-only and never become persistence authority.
- validation need: focused regression for cache-capacity overflow + unchanged write-back, edited list after eviction, and bounded handoff tracking.
- follow-up: preserve as a storage/plugin invariant when changing lazy manifests, plugin snapshot hydration, cache limits, or write-back reconciliation. Cross-link with `PLUGIN-MANIFEST-STALE-HYDRATED-WRITES-FAIL-CLOSED`, but do not merge them: stale-revision authority and cache-eviction authority are distinct failure modes.

### Why it matters

Commit `2981235e...` documents a real regression where `getDatabase()` hydrated more than eight lazy modules, the earliest full-manifest cache entries were evicted, and a later no-op plugin round-trip was misread as an asset edit. The correction records a bounded fingerprint of each list actually handed out and uses the LRU only as a fallback. This makes ordinary cache pressure semantically invisible to durable persistence.

### Autonomous progression

- historical review recorded at `notes/backfill-reviews/2026-09-03-0148-pocketrisu-plugin-writeback-authority-survives-lru-eviction.md`
- classification normalized to the current shared schema
- deduplicated against adjacent plugin-manifest invariants while preserving the distinct authority boundary
- Feature-ID dossier mirrored in helper repo
- no implementation branch / tests / personal PR: the invariant is already `ADOPTED` in official PocketRisu and remains present on current `develop`
- active-source forward cursors were not moved backward or changed in this run
- `HISTORICAL_BACKFILL_COMPLETE_THROUGH` not advanced because this bounded slice does not establish complete historical coverage across all tracked sources
