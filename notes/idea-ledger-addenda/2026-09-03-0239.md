# Idea ledger addendum — 2026-09-03 02:39 KST

## REQUEST-OBSERVABILITY-USES-EFFECTIVE-WIRE-MODEL-ID

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P1`
- lifecycle status: `ADOPTED`
- source evidence: `PocketRisu/PocketRisu` `764c62c903f0cc2d4d276dba2a3ec228733664e4`; current develop verification at `278251f85a19bfdfd4cf3faae780e62682878f9e`
- benefit: keeps request logs, generation/job metadata, previews, tool-loop results, streams, ordinary results, and failures tied to the provider-facing model id actually selected by routing rather than a mutable preset name or incomplete profile default
- conflict/risk: model-resolution failure still needs a safe identifying fallback; diagnostics must not broaden into credential logging
- validation need: user-valued model id, renamed preset, preview, tool loop, streaming/non-streaming, failure, malformed config fallback; all must agree on the same effective wire model identity
- follow-up: preserve single effective-model resolution and reuse across observability surfaces when request/preset routing changes

### Why it matters

`profileSnapshot.modelId` can be empty when a profile's model id comes from user values, while `preset.name` is only a mutable display label. The historical fix resolves `wireModel` once via the same routing invariant used for provider-facing identity and propagates it across all request result/logging paths. This prevents misleading post-hoc diagnostics after preset edits and keeps generation provenance aligned with the actual request.

### Dedupe

Kept separate from provider-capability and preset source-of-truth ideas. This item owns only the post-resolution observability identity invariant.

### Autonomous progression

Historical review and current-develop verification completed. Classified as already `ADOPTED`, so no implementation branch, tests, or personal-fork PR were created. Matching helper-repo invariant dossier is recorded separately.
