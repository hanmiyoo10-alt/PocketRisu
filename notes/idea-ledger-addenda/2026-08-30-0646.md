# Idea ledger addendum — 2026-08-30 06:46 KST

## BACKUP-INTEGRITY-READINESS-VERIFIER

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `MEDIUM`
- Size: `S`
- Evidence: `MEDIUM`
- Risk: `MEDIUM`
- Dependencies: PocketRisu backup/export authority map + explicit invariant inventory + non-mutating verification API + false-positive/false-negative failure tests
- Priority: `P1`
- Lifecycle: `DESIGN_NEEDED`
- Source evidence: `InoriNatsume/RisuVault@1d0d352fa6d93ba88629e30089bf38accf2c0fd5` (`risuvault verify` primitive and mandatory pre-publish gate)
- Benefit: make backup/export/recovery trust explicit by independently checking durable-state invariants before an artifact or state is treated as valid, reducing silent acceptance of incomplete or internally inconsistent snapshots.
- Conflict/risk: a verifier that shares the same faulty assumptions as the writer can give false confidence, while an over-broad verifier can block legitimate backups. It must be read-only, architecture-derived, and fail closed without repairing by guess.
- Validation need: reproduce at least one stale/missing/incoherent backup candidate; prove verifier rejects it without mutation; prove clean current-state export passes; cover false-positive cases; verify compatibility with existing save/integrity optimizations, no-op `flushServerDbKeepalive()`, and no forced `visibilitychange`/`pagehide` flush.
- Follow-up: map PocketRisu's exact backup/export authority and existing integrity checks, then draft a minimal read-only verifier contract. Do not port RisuVault encryption/storage architecture. If implementation is ever justified, first slice should be tests + pure verifier only, with no repair/migration behavior.

Dedup note: this complements `BACKUP-SNAPSHOT-DURABLE-STORE-BARRIER`. The barrier coordinates pending durable writers before snapshot creation; this verifier independently evaluates readiness/coherence before the result is blessed. Merge them into one future pipeline/design if both become applicable rather than creating duplicate backup flows.
