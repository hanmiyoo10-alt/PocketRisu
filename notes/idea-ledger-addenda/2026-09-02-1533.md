# Idea ledger addendum — 2026-09-02 15:33 KST

## `PLUGIN-STORAGE-PARTIAL-WRITES-DO-NOT-IMPLY-DELETE`

- `System impact`: `NO_SYSTEM_UPDATE`
- `Importance`: `HIGH`
- `Difficulty`: `LOW`
- `Size`: `XS`
- `Evidence`: `HIGH`
- `Risk`: `MEDIUM`
- `Dependencies`: `NONE`
- `Priority`: `P0`
- lifecycle status: `ADOPTED`
- source evidence: official `PocketRisu/PocketRisu@ebe32742a22b123eb0c52e4dc387d641090dee8a`; storage-externalization context `f0d4eee35ca0b4ac7e5fb4ee4668205b46723a44`
- benefit: prevents `getDatabase() -> setDatabase()/setDatabaseLite()` compatibility round-trips from deleting unrelated server-backed plugin keys when the exposed DB field is empty/partial
- conflict/risk: merge-by-default means omission cannot serve as deletion; destructive intent must use explicit per-key delete/clear APIs
- validation need: keep empty-object and partial-object regression tests; verify explicit clear/delete remain destructive, unrelated keys survive compatibility writes, and targeted V3 plugin reload remains intact
- follow-up: preserve omission-as-no-write semantics at this compatibility boundary during future plugin-storage/database refactors

### Evidence / rationale

After per-key plugin storage externalization, `db.pluginCustomStorage` remains `{}` in the main DB compatibility object. Replacement semantics therefore converted an incomplete compatibility view into destructive authority over the full backing store. Dogfooding reported a concrete 255-key -> 43-key -> 0-key wipe sequence. The adopted fix merges present keys only and leaves explicit `pluginStorage.clear()` as the full-wipe operation.

### Deduplication

Separate from `V2-PLUGIN-STORAGE-PRELOAD-FAILS-CLOSED`, optimistic storage rollback, targeted V3 reload, and plugin-update configuration migration. This item owns delete authority at the compatibility adapter: a missing key is not deletion intent.

### Progression

`ADOPTED`; no implementation branch/PR created. Durable historical review: `notes/backfill-reviews/2026-09-02-1533-pocketrisu-plugin-storage-merge-not-replace.md`.

`HISTORICAL_BACKFILL_COMPLETE_THROUGH` unchanged; this run proves only one bounded historical slice.