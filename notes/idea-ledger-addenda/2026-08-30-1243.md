# Idea ledger addendum — 2026-08-30 12:43 KST

## WORKING-SET-DURABLE-AUTHORITY-BOUNDARY

- `System impact`: `NO_SYSTEM_UPDATE`
- `Importance`: `MEDIUM`
- `Difficulty`: `MEDIUM`
- `Size`: `S`
- `Evidence`: `MEDIUM`
- `Risk`: `MEDIUM`
- `Dependencies`: matching PocketRisu reconstructed/disposable state owner + explicit authoritative durable source + reconstruction/publish contract tests
- `Priority`: `P2`
- lifecycle status: `HOLD`
- source evidence: `InoriNatsume/RisuVault` commit `12767efba3d6e824be05c1ea3a9bae2974cca8cf`
- benefit: preserves a clear authority boundary if PocketRisu later adds disposable hydration caches, compacted working sets, or externalized state; makes eviction/reconstruction safer and reduces the chance that an unloaded/missing working representation is mistaken for durable deletion.
- conflict/risk: copying RisuVault's encrypted git workspace architecture would be inappropriate and could create unnecessary storage complexity; authority semantics must be adapted to the actual PocketRisu owner rather than ported wholesale.
- validation need: identify a real PocketRisu owner where state is reconstructible/disposable; prove round-trip reconstruction, explicit publish semantics, and `NOT_LOADED != DELETE`; add crash/restart and stale-working-copy tests before any implementation.
- follow-up: keep as an architecture invariant/reference. If a matching owner appears, start with pure contract tests and an explicit authority resolver; do not change storage format, force DB flushes, or introduce a migration as the first slice.

### Deduplication note

This is related to but distinct from `DEFERRED-ROOT-WRITE-INTENT-GUARD`, inactive-chat working-set protection, plugin-storage lazy hydration, and backup integrity/readiness ideas. Those govern specific write, eviction, hydration, or backup mechanisms. This record captures the cross-cutting ownership rule that reconstructed working state is not automatically the durable authority.

### Historical coverage

The source commit predates the authoritative `InoriNatsume/RisuVault` forward cursor `1284cc93853bdba80fc3aab537fad2817d695914`, so the cursor is not moved backward. `HISTORICAL_BACKFILL_COMPLETE_THROUGH` remains unchanged because this bounded review does not establish complete coverage for every tracked source.
