# Idea ledger addendum — 2026-09-02 16:35 KST

## PLUGIN-STORAGE-CORRUPT-ROWS-ARE-QUARANTINED-UNTIL-REPAIR-SIGNAL

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `HIGH`
- Risk: `LOW`
- Dependencies: `NONE`
- Priority: `P1`
- lifecycle status: `ADOPTED`
- source evidence: `PocketRisu/PocketRisu@167def7df98e8272dcb179a4e8b4451e29e32604`; focused test demonstrates corrupt row is fetched once, skipped across repeated index refreshes, and becomes readable again after rewrite
- benefit: prevents unbounded periodic refetch/parse/log churn from an unchanged corrupt plugin-storage row without blocking healthy keys
- conflict/risk: quarantine must never imply durable deletion and must be invalidated on concrete repair/recreation signals, otherwise a repaired remote value could remain hidden
- validation need: one-fetch quarantine; healthy-key isolation; local rewrite clears quarantine; successful parse clears quarantine; server-index disappearance clears quarantine so later recreation can be fetched; reset/test isolation
- follow-up: preserve as an adopted storage reconciliation invariant; do not create a port PR because current official PocketRisu already contains the fix

### Dedupe

Distinct from `V2-PLUGIN-STORAGE-PRELOAD-FAILS-CLOSED` (runtime activation ownership) and `PLUGIN-STORAGE-PARTIAL-WRITES-DO-NOT-IMPLY-DELETE` (merge/delete authority). This item owns retry/revalidation behavior after one exact persisted key is known to be unparseable.
