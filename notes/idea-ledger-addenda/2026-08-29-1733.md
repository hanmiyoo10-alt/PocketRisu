# Idea ledger addendum — 2026-08-29 17:33 KST

## SENSITIVE-PORTABLE-DATA-DUAL-REPRESENTATION

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `MEDIUM`
- Difficulty: `HIGH`
- Size: `S`
- Evidence: `MEDIUM`
- Risk: `HIGH`
- Dependencies: matching PocketRisu-owned sensitive portable/reference-data boundary; explicit threat model and key lifecycle; filename/metadata leakage policy; authoritative-side and conflict semantics; atomic write/recovery guarantees; tests proving missing plaintext is not treated as deletion unless explicitly intended
- Priority: `P3`
- Lifecycle status: `DESIGN_NEEDED`
- Source evidence: `InoriNatsume/RisuVault` commit `d602bf8d268078a8d425432a217a8bdb12ffdf20`; `src/primitives/refs-sync.ts`; `src/primitives/refs-pull.ts`; source docs explicitly distinguish encrypted tracked files from plaintext gitignored work files and note filename leakage.
- Benefit: keeps a shareable/backed-up sensitive representation encrypted while allowing a bounded local plaintext working representation when a real PocketRisu owner requires it.
- Conflict/risk: security-sensitive persistent storage and key lifecycle; plaintext worktree expands local exposure; filenames and repository metadata remain visible; source-style delete-on-absence can become destructive if the plaintext side is stale/incomplete or is not authoritative.
- Validation need: first prove a concrete PocketRisu need and owner, then threat-model confidentiality/metadata leakage and define key derivation, rotation, recovery, versioning, atomic writes, partial-failure handling, stale-worktree conflicts, clone/restore, wrong-key/corruption behavior, and explicit deletion authority. Require round-trip and non-destructive sync tests.
- Follow-up: assistant-owned design dossier only. Do not port the source crypto/storage implementation and do not implement autonomously while this remains a security-sensitive storage design. If a matching owner emerges, decompose first work into read-only format/threat-model tests before persistent mutation.

Deduplication note: this is recorded as a storage/security representation-boundary idea, not as a generic backup/restore item. It should merge with a future PocketRisu-specific encrypted portable-data design if one appears rather than creating parallel crypto formats.
