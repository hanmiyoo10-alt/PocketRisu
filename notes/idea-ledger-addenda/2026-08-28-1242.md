# Idea ledger addendum — 2026-08-28 12:42 KST

## Stable-ID collection patching must not confuse reorder with internal edit

- System impact: `NO_SYSTEM_UPDATE`
- Importance: `HIGH`
- Difficulty: `LOW`
- Size: `XS`
- Evidence: `MEDIUM`
- Risk: `MEDIUM`
- Dependencies: matching PocketRisu stable-ID array patch/diff owner; current personal-fork code search does not expose Alter's `RisuSavePatcher` / `diffArrayWithIdGuard` boundary
- Priority: `P0`
- Lifecycle status: `HOLD`
- Source evidence: `PocketRisu-Alter/PocketRisu-Alter` `79c35cf2594dc20dd7334f3ca18ea752678a189e` — bot presets had gained stable IDs, but length-only positional patching still treated same-length reorder as per-index edits; the fix switched to ID-guarded structural detection and added reorder/missing-ID/duplicate-ID regression coverage.
- PocketRisu benefit: prevents silent cross-entity save drift if incremental patching is applied to stable-ID collections.
- Main conflict/risk: the current PocketRisu fork does not expose the same patcher architecture; introducing a generic abstraction just to mirror Alter would add unjustified complexity, and structural replacement may increase write size.
- Validation need: at a concrete matching owner, test internal edit vs reorder, reorder+edit, add/delete, missing/duplicate IDs, and legacy pre-ID data; identity mismatch must never produce per-index patches across different logical entities.
- Follow-up: retain as a save-integrity invariant; promote only when a concrete PocketRisu stable-ID patch owner exists. Historical backfill record: `notes/backfill-reviews/2026-08-28-1242-pocketrisu-alter-stable-id-array-diff.md`.
